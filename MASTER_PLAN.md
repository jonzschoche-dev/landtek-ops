# LeoLandTek — Master Plan (single source of truth)

> **THIS IS THE ONLY AUTHORITATIVE PLAN. Update it in place.**
> Do not create parallel directive / roadmap / status / deployment-plan docs. That sprawl —
> and stale "READ FIRST" pointers into it — is what kept dragging every new session back to a
> May-2026 reality and causing knowledge regression. `CLAUDE.md` points here as READ FIRST.
>
> **Last updated:** 2026-06-12 — consolidates 9 prior planning docs (now in `archive/planning-2026-06/`).
> **Provenance discipline applies to THIS doc too.** Facts are tagged **✅ VERIFIED** (checked against the
> live system this session) or **⚠️ UNVERIFIED** (carried from May-2026 docs, not re-checked — confirm
> before relying). Never let an unverified carry-over harden into an asserted fact.

---

## 1. North star & live legal posture

**✅ Aug 12, 2026 — Jonathan Zschoche testifies as Patricia Keesey Zschoche's witness** in **Civil Case 26-360 (Zschoche v. Balane)** (2,587 sqm parcel recovery, MTC Mercedes).

- **✅ Court: MTC Mercedes, Camarines Norte; governed by Summary Procedure.** NOT the RTC. NOT an "Aug 1 pre-trial." Pre-trial was **May 13, 2026 (passed)** — the case advanced into motion practice.
- **✅ Live now:** our **Motion for Summary Judgment** pending a ruling; defendants moving to admit **Gloria Balane's Judicial Affidavit** (excluded at pre-trial); **mediation** held ~June 2.
- **✅ Parallel: MWK-GUARDIANSHIP** (special proceeding, RTC Daet) — **✅ Atty. Adan Botor is counsel of record** (ENGAGED); **✅ petition FILED as Spec. Proc. No. 2680, RTC Br. 41 Daet — hearing SET 2026-07-27 8:30am** (in-court; per RTC Order doc#1199 / gmail#91604 from Botor, recv 2026-06-22; supersedes the older "pending filing" language). Guardianship-first sequencing (Jonathan as guardian) gates CV-26360 authority.
- **Posture: wartime, not peacetime.** Everything the stack does serves the Aug 12 deadline / the SJ motion / the guardianship grant, or it is deferred. No from-scratch rebuilds during the live window.

**✅ Verified case corrections (override older docs / CLAUDE.md):**
- Balane's title = **T-079-2021002126** (not …2127).
- Operative instrument = the **16 Mar 1992 SPA** to de la Fuente (revoked **2005**, *published* only **2020**).
- SJ kill-shot = that 1992 SPA grants only "negotiate," **not "sell"** → sale is **void**, good faith irrelevant (*nemo dat quod non habet*). Backup prong: revocation + duty of inquiry (*Yoshizaki*).

## 2. Active deliverable

**✅ Balane Evidence Spine — `case_work/MWK-001/BALANE_EVIDENCE_SPINE.md`.** Source-grounded void-chain, both sides' arguments, and a cross-examination map built from Gloria Balane's own affidavit (doc 1089). Feeds the SJ motion + Aug 12 testimony.
- **Next:** SJ/trial exhibit list + cross-exam outline; obtain the **2016 Deed of Absolute Sale** to Balane (CTC from RD Daet — not yet in corpus); vision-OCR the 1999/revocation images (docs 1132–1134); close the T-32917 → T-52540 chain hole.

## 3. Current verified state (2026-06-12)

- **✅ Infra:** single DigitalOcean droplet — **1 vCPU / 2 GB / 67 GB NVMe + 2 GB swap** (39% disk), Premium Intel, $16/mo (upgraded 2026-06-13 from 1 GB / 33 GB; load dropped 1.8→0.5 — **freeze risk resolved**, single vCPU adequate). Postgres in docker `n8n-postgres-1` (`172.18.0.3:5432/n8n`). Repo `landtek-ops`, pushed from Mac (`~/landtek`) + VPS (`/root/landtek`). Tailscale SSH (check-mode reauth).
- **✅ Corpus:** ~**1,056 canonical** docs (1,126 total). All 26-360 court filings are blended in from Barandon's emails, incl. **both sides' judicial affidavits**. **~791 MWK-001 docs sit at `pending_classification`** (text captured, integration layer unfinished); 95 in `error`.
- **✅ Daemons active:** `leo-simulator`, `landtek-truth-loop`, `landtek-fullstack-loop`, `landtek-corpus-backfill`, `landtek-tg-router/-inbox/-media`. **`truth-qa-loop` INACTIVE.**
- **✅ Ingestion:** Gemini vision OCR (free tier — exhausted) + **local Tesseract via PyMuPDF** (the cost-driven pivot). Qdrant Cloud embeddings (gemini-embedding-001).
- **✅ Reasoning:** Claude `sonnet-4-5`; `truth_negotiator` + `truth_judge` + `claim_truth_verdicts`; `_safe` views; provenance grading (verified / inferred_strong / inferred_weak).

**✅➜ COST — liability largely RESOLVED (re-grounded 2026-07-05).** The old "~$40/day invisible burn" was the **simulator** through n8n's Anthropic node — but the sim is kept **OFF** (standing rule, 2026-06-14) and Anthropic credits are **depleted** (API returns 400 "credit balance too low"), so recorded spend is now **~$0.15 / 7 days** (only `truth_negotiator`'s challenger calls, per the `llm_calls` ledger). The primary tier is the **free local Ollama** (~99% of inference; see the §3 Inference row / `ARCHITECTURE.md` §5). Ongoing visibility: `anthropic_spend_bridge` (deploy_427) + a **daily-digest spend line** (deploy_697) so cost never goes dark again. **Residual blind spot (now moot):** n8n LangChain-node spend still isn't itemized in `llm_calls` — irrelevant while the sim is off and credits are depleted, but **re-instrument before re-enabling the sim or topping up Anthropic credits.**

## 4. Strategic frame (carried forward — still sound)

- **Vision:** the most truth-seeking, deadline-reliable property + legal-ops AI; database is exhaust, the workspace is the product.
- **9 non-negotiable principles:** (1) no hallucination, (2) never miss a deadline, (3) comms chokepoint sacred (S14 Telegram rules), (4) `provenance_level` sacred, (5) information is gold, (6) self-research, (7) git discipline, (8) hard cost stop, **(9) inference is marked** — whenever any layer (OCR, comprehend, RAG, bible, brief, Telegram) substitutes inferred content for source content (even high-confidence), the inferred token is flagged inline per §4B; silent substitution is the failure that compounds into hallucination; no layer exempt — provenance applies inside the prose, not only on the row. **⚠️ Principles 8 and 9 have known violations in practice — see §3 (cost telemetry) and `drafts/ocr_pending_write/*.flags.md` (silent OCR corrections, since rewritten).**
- **5 elite layers:** per-matter · per-portfolio · per-firm · per-channel · per-jurisdiction.
- **Phases:** 0 safety substrate · 1 data integrity · 2 relational + financial · 3 autonomous agency · 4 channels · 5 multi-jurisdiction. **⚠️ All May-2026 phase timelines are obsolete — rebaseline against Aug 12.**

## 4A. North-Star vision — the full product (7 pillars · from artifact v1.1, 2026-06-13)

The complete arc Leo is built toward (vision doc: **claude.ai artifact `1893ca64`, v1.1**).
MASTER_PLAN is the *execution* truth; that artifact is the *vision* truth — keep them linked.

**5 readiness requirements:** Grounded · Durable · Complete-corpus · Proactive · Affordable.

**7 capability pillars** (status grounded in the box, 2026-06-13):
1. **Evidence & Knowledge** — grounded memory, citations, immutable assertions. **Built (strong).**
2. **Legal Case Mgmt** — matters, chain of title, deadlines, evidence spine. **Active (the Aug-12 focus).**
3. **Finance & Accounting** — ledger, bill extraction, P&L, per-matter ROI. **Planned v1.5** (QuickBooks integration available in-env).
4. **Property Mgmt** — tenants, rent, leases, maintenance, permits. **Planned v2.0.**
5. **Proactive Intelligence** — daily briefs, sentinels, autonomous discovery. **Partial** (`daily_digest` + sentinels exist).
6. **Forensic & Compliance** — signature/authentication, audit chain, OpenTimestamps. **Early** (`fraud_indicators` + audit exist; signature-validation not built).
7. **Platform & Access** — multi-client, 4-layer RBAC, billing, omnichannel. **Partial** (client isolation + auth-gate exist).

**Net-new capability dimensions (were NOT in MASTER_PLAN):**
- **Geospatial / Mapping — ◐ v1 SHIPPED (deploy_683, 2026-07-04).** Client-facing property maps: a client opens a token-gated mobile map, sees their parcel drawn on satellite, and taps "Locate me" to see whether they're standing **inside their boundary** and how far to the nearest edge (device GPS + on-device point-in-polygon). **Infra resolution:** rather than stand up a separate PostGIS DB, geometry is stored as **GeoJSON in JSONB** on the production DB (no PostGIS extension), with area/centroid/overlap math in pure Python (`leo_tools/geo_math.py`). Accuracy is a first-class provenance tier — `rough` (hand-placed on satellite, always renders an "APPROXIMATE — not a survey" banner) → `survey` → `ortho` (the <1m drone orthomosaic upgrade path). Pieces: `map_parcels` table + `map_parcels_client` view · `leo_tools/mapping.py` (ops draw tool `/ops/map`, client map `/client/<token>/map`) · `scripts/mapping_agent.py` (plot-vs-title area audit + cross-owner overlap/encroachment leads) · `mapping-agent` subagent · daily `landtek-parcel-audit.timer`. **NB — `map_parcels` (absolute WGS84, placed on the world) is a DIFFERENT layer from the pre-existing `parcels` table (scripts/parcels.py — relative local-meter survey shape from metes-and-bounds, un-georeferenced); the bridge = georeference a `parcels` shape via a tie point → a `survey`-tier `map_parcels` row.** **Switch held OFF** — seeded on MWK-BALANE for internal proof; no client link handed out until Jonathan says ready. Still ahead: metes-and-bounds auto-plot from the ~65 survey docs (needs tie-point coords), orthomosaic tiling. *(Client-separation via `client_code` FK; never fabricate a coordinate — unplotted parcels stay `awaiting_plot` with NULL geometry.)*
- **Multimodal vision** — Claude/Gemini vision, object detection, EXIF + perceptual hashing (feeds Forensic).
- **Omnichannel** — Telegram (live) → Email → Messenger/WhatsApp/Viber via one channel-abstraction layer.
- **Agentic calendar** — derive obligations from docs, work backward from hard deadlines, lead-time alerts.

**Model-cost ladder (routing target ≈ 70% inference cut):** Opus $15/$75 (hard legal synthesis only) · Sonnet $3/$15 (default) · Haiku $0.80/$4 (classify/route) · GPT-4o-mini $0.15/$0.60 (bulk extract) · Gemini Flash $0.075/$0.30 (embeddings).

**OCR (correction vs artifact):** running stack is **Gemini vision (free tier) + Tesseract**; "Google Document AI" is a *planned premium-tier upgrade*, not current.

**Proof clients before GA:** MWK-001 (title recovery) + Paracale-001 (estate/mining/construction). Targets: $6–15/day burn · $15–80/mo per-client inference · PHP 15–50k/mo retainer · >85% margin.

**Build-status snapshot — NOW vs DELIVERY (2026-06-27, senior-engineer audit).** `●` live · `◐` built/inert · `○` scaffold/planned. Mirrors §6's roadmap — update both together.

| Pillar / system | NOW | DELIVERY |
|---|---|---|
| 1 Evidence & Knowledge | ● 3-tier write-gate (triggers) + excerpt-gate; verify_worker (local Ollama) + verify_loop; **18-act law corpus**; rag_local 1055/9451; knowledge meter; Constitution | answer-gate wired into Leo; ~95% corpus readable |
| 2 Legal Case Mgmt | ● 33 matters; **Grounded Matter Engine** (fact graph + element→gap matrix + freshness); title-chain root→tip; forum desks + 11-forum mandate + router; deadlines; dossier pipeline | play/strategy/revenue live-feed Leo; per-client status/next-action |
| 3 Finance (v1.5) | ◐ finance_transactions, v_matter_pnl/roi, QuickBooks MCP in-env | ledger, bill-extract, P&L, per-matter ROI, retainer billing |
| 4 Property (v2.0) | ○ property_assets (83 rows) | tenants/rent/leases/maintenance/permits |
| 5 Proactive Intel | ● daily digest (7AM); deadlines (countdown); filing_monitor (6h); email_briefer (trigger-bug fixed deploy_613) | 8-agent control plane; per-**client** alerts; one-tap daily proposals |
| 6 Forensic | ◐ fraud_indicators; forensic_hash (sha256+phash+EXIF); audit chain | signature/auth validation, OpenTimestamps |
| 7 Platform & Access | ◐ client-separation + sentinel; auth-gate; /ops cockpit; /files/c; channel endpoints | 4-layer RBAC, multi-client, billing, omnichannel live, per-client Leo |
| + Mapping (geospatial) | ◐ v1 shipped deploy_683 — `map_parcels` (GeoJSON/JSONB, no PostGIS; distinct from the relative `parcels` survey table); ops draw tool `/ops/map`; token-gated client map `/client/<t>/map` + "Locate me" inside/outside; `mapping_agent` area+overlap audit; MWK-BALANE seeded, switch OFF | metes-and-bounds auto-plot from survey docs; drone orthomosaic (<1m) tiling → `ortho` tier; roll out to Paracale/NIBDC |
| Inference + cost | ● sovereign Ollama 7B/14B ($0 unlimited); model-router (~70% cut); offline core; spend-bridge (built, **disabled**) | spend-bridge enabled (cap on TOTAL burn); frontier on credits for hard synthesis only |
| Leo (delivery surface) | ◐ gate + discernment protocol + command-set built; **n8n workflow INACTIVE** | live control plane: HIGH/MED/LOW alerts, approval flow, 4h→Barandon/12h→Botor/24h→pause escalation, every reply gate-passed |

**8-strategic-agent fleet — WIRE/REUSE map (don't rebuild; 2026-06-27 verified against the live box).** Wave 1 is ~90% already built — the deliverable is *wiring to Leo*, not new agents.

| Blueprint agent | Maps to (already exists) | Status / remaining |
|---|---|---|
| W1 Discovery | `filing_monitor.py` (6h timer ENABLED) | ● built — wire → Leo HIGH alert |
| W1 Execution-Tracking | `execution_tracker.py` (on-demand) | ◐ built — add 12h timer + Leo wiring |
| W1 Deadline-Orchestration | `deadlines.py` (in digest) | ◐ built — add escalation + Leo wiring |
| W1 Narrative-Generation | `brief_drafter`+`legal_agent`+`case_synthesizer` | ● built (richer than spec) — wire as Leo `/generate` trigger |
| W2 Cascade-Verification | `keystones` (2) + `strategy_engine` | ◐ scaffold — build re-verify-on-change agent |
| W2 Cost-Outcome | `legal_outcome_estimates`/`legal_cost_actuals` (empty) + `revenue_engine` | ○ tables scaffolded — build EV agent |
| W2 Settlement-Valuation | `dominion_value_estimates` (empty) + `property_assets` (83) | ○ data present — build valuation agent |
| W2 Opponent-Modeling | — | ◷ greenfield — the only true new build |

So the lean delivery: **Wave 1 = wire 4 existing agents (+1 timer, +escalation) to Leo; Wave 2 = 1 greenfield + 3 agents over existing scaffolds.** The substrate (`keystones`, `matter_objectives` 19, `property_assets` 83) is already live. The empty pillar tables (finance/valuation/outcome) are **architecture-first scaffolds (§6.5), NOT bloat** — a prior bloat-audit wrongly flagged them; do not drop them.

## 4B. Conventions — inline inference marking (Principle 9)

Any cross-layer output that substitutes inferred content for source content uses ONE compact, machine-parseable tag. Tag every NAMED ENTITY, DOLLAR AMOUNT, INSTRUMENT REF (doc/page/book/notary), DATE, and QUOTED PHRASE; common-word OCR fixes (`tho`→`the`) may be silent — this is about facts, not grammar.

| Tag | When | Example |
|---|---|---|
| `[OCR: raw]` | OCR-noise correction | `CESAR M. DE LA FUENTE [OCR: CESAR N. DE LA VENTE]` |
| `[?word]` | missing/inferred, no raw source | `[?Bounded] on the` |
| `[v: source]` | verified cross-reference | `KEESEY [v: birth cert; RTC Order; 307 corpus]` |
| `[STRUCTURE: desc]` | reconstructed table/list | `[STRUCTURE: footer table reconstructed]` |
| `[HUMAN VERIFY]` | needs human confirmation | `Yanto/Yu — uncorroborated [HUMAN VERIFY]` |

Parse: `\[(?:OCR|STRUCTURE|v|HUMAN VERIFY|\?)[^\]]*\]`. Layers must PRESERVE upstream tags (OCR→comprehend→bible→brief); stripping them is a violation. Raw OCR noise (bearings / lot codes the OCR mangled) stays in a verbatim `[RAW OCR DUMP] … [end]` block, never silently cleaned.

## 5. Operating model (multi-agent — condensed; full git protocol in CLAUDE.md)

- **Two agents:** VPS Claude (`/root/landtek`, runtime/ops) + Mac Claude (`~/landtek`, authoring/design). Both push to `landtek-ops`. Mac auto-sync launchd pulls every 2 min (fast-forward only).
- **Git discipline:** `pull --rebase` before work; commit **specific paths** (never `git add .`); deploy via `scripts/landtek_git_routine.sh deploy NN "title" paths`; never force-push `main`; never commit secrets (`.env`, `*.pem`, `google-creds.json`).
- **Deploy pipeline:** clone-first author → diff review → `truth_tests` gate → commit `deploy_NN` → `cowork-bridge` executes batch scripts (`leolandtek-deploys/inbox/`) → heartbeat watch.
- **Safety substrate:** provenance ontology = master of truth; `_safe` views for any legal output; never break comms; auth-gate every access; clone-first authoring.
- **Health invariants:** daemons active · heartbeat < 1 hr · Telegram bot responds · n8n active.

## 5A. Active Cowork directive (2026-06-14 — two windows, one working tree)

**Roles.** Two Cowork windows run on this same Mac tree:
- **EXECUTOR** — the window with VPS SSH + local Drive files + Chrome + DB. Owns: running OCR/comprehension/embeddings, DB write-backs, deploys, and **all commits/pushes**.
- **DESIGNER** — the sandboxed window (no VPS/DB/local files/browser). Owns: **drafting/refining files only**; announces each new file so the executor rescues + commits it. The designer does **not** commit, push, or run anything needing the VPS/DB.
- **Git:** one committer (the executor); `pull --rebase` before every commit; specific paths only; never `git add .`.

**Do NOT rebuild — these already exist (deploys 442–472):** freshness / evidence-matrix / play / strategy / revenue engines; `harvest_facts.py` (free regex facts); `comprehend.py` + `comprehend_browser_adapter.py` (text→facts); `ocr_browser_adapter.py` (image→text); `rag_embed.py` (pgvector RAG); `knowledge_coverage.py` (the awareness meter).

**Standing rules (load-bearing — do not regress):**
- Awareness is an **accretion** problem, not a budget problem. Spend only where it moves `knowledge_coverage` (live — read it, don't quote a fixed number). The **simulator stays dead** (it was the money pit).
- **Thinking runs on the Claude Cowork subscription**, not the depleted metered API. Embeddings = Gemini-free / local. **OCR engine (REVISED 2026-06-14): Claude vision direct on the local PDFs for legal-critical docs** — operator found Gemini browser OCR **not accurate enough for exhibit-grade text** (it missed the deed/SPA dates, mis-attributed the notary, and was ambiguous on the KELSEY/KEESEY name; Claude vision on the original corrected all three). Gemini browser OCR is now relegated to low-value searchable bulk only.
- The corpus is **local on the Mac** (`…/My Drive/LANDTEK`, ~950 files) → Claude vision OCRs directly (T-4497 etc. that were Drive-only on the VPS are readable here).
- **Transport-agnostic ingestion contract:** DB `--next` / `--write`, any LLM in between. *Extend* transports (ChatGPT/Grok browser); do **not** fork the contract or the prompts (`comprehend.PROMPT`, `reocr_gemini.PROMPT` are single-source).

**Current lanes (parallel, no contention):**
- **Executor:** Claude-direct comprehension of readable titles + RAG embeddings + applying the operator's OCR write-backs.
- **Operator:** Gemini Advanced browser OCR of the garbage docs (worklist provided; `ocr_browser_adapter.py --write-ocr` flows it back).
- **Designer:** refine the OCR/comprehend prompts; script the full OCR-worklist generator; add ChatGPT/Grok browser transports to the adapter; sharpen the element frameworks. **Design only — no execution.**

### OCR attack (2026-06-14 — SUPERSEDED by §6B; kept for history)

> **Superseded 2026-07-07 by the Live-Layer master plan (§6B).** "Connected" is now the deploy_712
> `connect-verify` 5-signal definition, not the "drain 341 flagged" frame below. The remediation/OCR
> work continues under §6B W1 (earns provenance) + W2 (deterministic connect). Text below is historical.

- **Engine (REVISED 2026-06-14 — Gemini not accurate enough for exhibits):** legal-critical docs (titles / SPAs / deeds — Waves 1–2) → **Claude vision direct on the local PDF** (executor session; reads + OCRs + comprehends in one pass; most accurate, $0 on the subscription). Low-value searchable bulk (Wave 3 images/surveys) → Gemini browser is acceptable. **Preprocess faded paper titles first: `ocr_preprocess.py --variants gray`, then read the GRAY image** — validated on doc 87 (gray = grayscale+autocontrast+unsharp+denoise reads cleanly; blue-channel isolation OVER-THINS faded ink into fragments; reserve blue for saturated modern blue-ink). Every result flows back through `scripts/ocr_browser_adapter.py` (re-scores quality, backs up old text, feeds RAG + comprehension). Goal: `--status` pending_ocr **341 → 0**; `knowledge_coverage` "documents readable" **66% → ~95%**.
- **Priority (already encoded in `--next-ocr`):** text-bearing garbage first (titles / SPAs / deeds), worst score first; then no-text images/surveys; skip zip/docx/xlsx (handled elsewhere).
- **Loop per doc:** `python3 scripts/ocr_browser_adapter.py --next-ocr 5` → locate the local file under `…/My Drive/LANDTEK ` (executor supplies the doc→path worklist) → upload to Gemini Advanced + paste the **canonical prompt** (`reocr_gemini.PROMPT` — faithful transcription, preserve names / dates / title nos / bearings exactly, **no paraphrase**) → copy the transcription → `echo "<text>" | python3 scripts/ocr_browser_adapter.py --write-ocr --doc <id>`. One doc per chat; transcribe **all pages** of multi-page PDFs. If write-back still scores flagged (<0.30), the OCR didn't take — retry.
- **Waves.** W1 (keystone): SPA Cesar de la Fuente (246) · T-4497 family (25 / 382 / 348 / 97) · TCT-4503 (87) · Victa-fraud T-47657 set (28 / 31 / 34) · OCT/TCT-1616 (639). W2: remaining flagged titles/deeds. W3: no-text surveys/images.
- **Roles for the attack:** operator drives the browser loop · executor supplies the worklist + runs write-backs + comprehends/embeds results + tracks the meter · designer scripts the full doc→local-path worklist generator, adds ChatGPT/Grok transports for parallel lanes, and refines the prompt. Round-robin subscriptions once the extra transports exist; executor can Claude-direct the few hardest docs from the local files if Gemini stalls.

## 6. Roadmap (rebaselined to Aug 12 — legal deliverable first)

1. **✅ in progress — Balane SJ/testimony pack** (spine → exhibit list → cross-exam outline). The only calendar-bound work; everything else yields to it.
2. **✅ Cost governance — DONE (deploy_427).** `scripts/anthropic_spend_bridge.py` records n8n/simulator token usage into `llm_spend`, so `cost_governor.can_afford()` now gates on TOTAL burn. Validated: 25 sim execs = $2.82 (~$0.11/probe, ~32k input tokens each — *that* was the ~$40/day). Live cockpit panel at `/ops/spend` (deploy_428). The cap becomes real the moment the bridge timer is enabled (see Activation).
3. **Data integrity** — title-chain holes + live-filing exhibit-tier still open. **Corpus backlog (re-assessed 2026-06-13):** 938 docs at `pending_classification` = 703 OCR-able digital docs the backfill daemon isn't draining (idle despite eligible — needs a runtime look at its Qdrant-seed startup) + ~54 needing Drive/Gmail re-fetch + ~50 unsupported formats (.eml/.docx/zip/corrupt). **No automated `pending→classified` transition exists** (only one-shot `scannerpro_ingest.py` sets it); the missing classify step is LLM-dependent = credit-gated. This is an *operational* drain, not a creditless build.
4. **Carried-over "blockers" — RE-VERIFIED 2026-06-13 (mostly de-haunted):** heartbeat dashboard ✅ exists (`/ops/health` + `/ops/spend`); auth-gate ✅ exists (`leo_tools/server.py`, `channel_adapters.py`; `/files/c/` is intentionally public); DSN "fragmentation" benign (3 env-var names → one DB); Bible pipeline built (`generate_case_bible.py` + `opus_validate_bible.py`) — only the Opus audit *verdict* is unverified (needs an Opus run = credit-gated); **auto-rollback sentinel genuinely not built** (mitigated by the truth_tests deploy gate + manual `git revert` — low priority). `leo_qa_runner` v2-stub kinds fixed (deploy_429).
5. **Web workspace v1** — deferred (peacetime; the server-rendered Flask cockpit at `/ops` suffices for now; removes the Termius dependency only when the live matter is clear).
6. **✅ Cold infra built (2026-06-13, deploys 431–439) — "build everything" architecture-first, zero API spend:**
   - **Trajectory dashboard** `/ops/trajectory` (mission control: pillars, build %, gates, live signals).
   - **Geospatial** — `survey_geometry.py` (metes-and-bounds→polygon+area), `parcels.py` (+ `/ops/parcels` map, PostGIS-ready WKT), `survey_vision_extract.py` (Gemini-vision pulls courses off plan scans — **validated e2e: 37 courses read off a scan, free-tier**). PostGIS deferred until georeferencing (tie points); per-lot segmentation is the next refinement.
   - **Model-routing ladder** `model_router.py` — tier→model w/ fallback; **validated: classify fell to Gemini Flash and answered with Anthropic at $0** (the ~70% efficiency lever + graceful degradation).
   - **Finance scaffold** — `finance_transactions` + `v_matter_pnl`/`v_matter_roi`.
   - **Forensic** — `forensic_hash.py` (sha256 + perceptual + EXIF; found 7 near-dup groups).
   - **Email channel** — `email_channel_bridge.py` feeds Gmail into the existing `/api/channel/email` adapter (inbound + send drain).
   - LLM-runtime *behaviour* of the gated pieces (Leo replies, classify-at-scale, bill-extraction, reply content) lights up via `activate_stack.sh` on credit top-up. The scaffolding is built + inert.

## 6A. Reliability-first plan (2026-06-20 — counter to the "build the apex agent" pitch)

Operator surfaced the real failure: *"the stack is missing every important date — it's almost
useless; a random LLM helps more."* True diagnosis: the stack is **~90% built but unreliable +
not proactive**, not greenfield. So the plan is **reliability before autonomy**, $0-first, on the
existing machinery — explicitly rejecting the rebuild / always-on-loops / **self-deploying-code**
("Meta-Evolution") / Jetson framing (bottleneck is grounded data + credits, not compute).

**5 pillars** (each builds on what exists; metric-gated):
1. **Awareness & data quality** — close the deadline gap + raise *verified*-fact coverage on the
   live matters first. `deadlines.py` (deploy_496/497) surfaces every dated obligation with a
   forward-marker precision filter (no filing-date false alarms) and classifies the 27 dateless
   matters honestly: **17 genuinely need a date** (incl. GUARDIANSHIP pending-filing + the ARTA
   cases), 6 watch-only by design, 4 AUTO orphans. Metric: deadline coverage + awareness score.
2. **Proactivity (push, not pull)** — `build_digest.py` now **leads the daily digest with "Due
   dates"** (north-star countdown, overdue-confirm, upcoming, needs-a-date count). The stack tells
   you what's due unprompted. $0.
3. **Grounding discipline** — Constitution + `leo_answer_gate.py` (built/tested) + structured tools
   keep outputs on exact data; dates/facts go via structured leo-tools endpoints, **not** the
   vector stores (RAG/`rag_local`/Qdrant stay for *semantic doc* retrieval — embedding exact dates
   loses precision and invites "close-enough" hallucination).
3.5 **Integration into live Leo** — the deterministic engines write structured Postgres rows;
   Leo reads them through leo-tools HTTP tools (`/api/deadlines`, `/api/get_verified_facts`) like
   the existing `query_documents`/`get_entity` nodes. Build now ($0); wire at activation.
4. **Earn autonomy slowly** — ladder: read-only digests → **auto-suggest via the existing
   proposals queue** (`leo_improvement_proposals`/`promote_proposals.py`, human-approved) → execute
   only the lowest-risk, each rung **metric-gated** (e.g. deadline coverage ≥90% + push clean ~2wk).
   **Never auto-execute before the gate; never self-deploying code.**
5. **Stay cheap** — $0 deterministic engines do standing work; LLM only where it moves a metric;
   sim stays dead; trim the 90KB Leo systemMessage at activation (biggest per-call token waste).

**Awareness dashboard:** extend the existing `/ops` cockpit with `/ops/awareness` (coverage trends
from `awareness_log` + `surfaced_deadlines` + per-matter verified-fact coverage) — at-a-glance, $0.

**Done this session (P1+P2, $0):** honest deadline coverage + proactive daily push. **Next ($0):**
`/ops/awareness` page + the structured `/api/deadlines` feed; then the credit-gated comprehension
pass to raise verified-fact coverage.

## 6B. Live Layer — Corpus Connectivity master plan (2026-07-07)

**Scope.** The runtime that *mutates the corpus* — intake, OCR remediation, connectivity, provenance-
earning. Owned by the live-layer agent; the ontology layer (`ONTOLOGY.md` / validators) is a **separate
desk, fed by directive, never edited from here.** Supersedes the 2026-06-14 "OCR attack" op (§5A) with the
`connect-verify` framing.

**North star.** Every matter's corpus reaches **fully-connected = the 5 `connect-verify` signals** (text ·
provenance `model_used` · `ocr_quality` · `document_type` · embedded), with **provenance EARNED, never
faked**, sequenced by evidentiary value for Aug 12. Baseline (2026-07-06): **86/1579** connected corpus-
wide, **0/388** Paracale. Authoritative "done" runbook = `docs/INGESTION_DIRECTIVE.md` (a 6-signal superset).

**Binding constraint = provenance.** 4 of 5 signals are deterministic + already automated (quality/type/embed
via `case_corpus_sweep.sh` §3.5 + Mac `com.landtek.embed`, deploy_715/716). Only **provenance** — and text for
a degraded doc — is *earned* by a real engine read. Paracale = 0 earned; corpus = 86 (truthful
`extraction_runs` backfill). So the flagship job is the remediation tier that **earns** provenance; nothing
else moves Paracale off zero. Fabricating `model_used` to pass the gate is forbidden (ontology A8).

**Workstreams (prioritized):**
1. **W1 — OCR Remediation Tier (flagship; earns provenance + text).** Tier-4 image path (`ocr_preprocess`
   gray/blue/bw@450 → frontier-vision read → **reconcile to the doc's own totals**), keystone-first. One pass
   writes text + **stamps `model_used`** + re-scores quality + re-embeds + types → passes `connect-verify` →
   **certify (T3 human)** via the `ocr_remediation` work-kind. Building it = pipeline-desk work; the live
   layer plans + sequences it. Stays a drip within OCR-ladder quota (no Gemini-free-as-primary, no 72B).
2. **W2 — Deterministic connect (keep + extend).** §3.5 sweep + Mac embed are live/creditless. Extend
   `document_type` to Paracale's **71 unclassified** via a local qwen classify pass → the deterministic map
   then types them. Keep sweep/embed timers green (`systemctl --failed` = 0).
3. **W3 — Embedded-source reconciliation.** Gate reads `corpus_backfill_state.embedded` (1489); embedder
   writes `rag_local` (1492) → **3 diverge**. Make `corpus_backfill_state.embedded` canonical + have the
   embedder set it. Removes false gate-holds.
4. **W4 — Measurement as a runtime signal.** Per-matter live dashboard over the exact 5 gate signals, logged
   each sweep so "connected %" must move (like `awareness_log`). One definition — the gate's, never a second.
5. **W5 — Matter priority.** MWK (Aug-12 docs) first, Paracale second; within a matter, keystone/element-
   proving docs first — connectivity spend buys evidentiary value.

**Sequencing.** *Now:* W2 (Paracale 71 classify) · W3 (embed-source fix) · W4 (dashboard). *Next:* W1 stood up
on a small keystone batch to prove the earn-provenance loop end-to-end, then scale by W5. *Continuous:* the
deterministic sweep + embed keep the 4 cheap signals current on every ingest.

**Metrics.** Provenance-earned rising off 86/0 (headline) · fully-connected % up each sweep · **zero fabricated
provenance** · timers green, `connect-verify` hold-rate falling as docs genuinely connect.

**Handoff to the ontology layer (by directive, not self-edit):** formalize §2.8 ConnectedDocument + invariants
A7–A9, retire the "`model_used`=0" premise, ratify the canonical embedded source. Directive issued 2026-07-07.

## 6.5 Activation — flip the stack ON when credits land (architecture is in place)

Everything is built cold and **inert**: the spend-bridge timer is disabled, the synthetic loops
are paused/disabled, Leo answers only once `ANTHROPIC_API_KEY` has balance. Construction cost $0;
only activation consumes tokens. One safe, ordered, idempotent step — run on the VPS from `/root/landtek`:

    ./scripts/activate_stack.sh                  # DRY RUN — prints the plan, changes nothing
    ./scripts/activate_stack.sh --go             # bridge FIRST, then truth-loop + fullstack-loop
    ./scripts/activate_stack.sh --go --with-sim  # ALSO re-enable the simulator (opt-in; the big burner)

Order is enforced for a reason: the **cost-metering bridge goes live before the loops**, or they burn
invisibly again (the outage). The simulator is opt-in (~$47/day, ~1 verified improvement per 1,744
probes — decide per §7 first; consider a wider cycle / lower `LANDTEK_DAILY_LLM_CAP`). After running,
confirm `/ops/spend` shows recorded n8n spend + the cap enforcing, and that Leo replies.

## 7. Open decisions for Jonathan (still live)

- The simulator burn — throttle, instrument, or cap? (§3)
- ARTA cases: 9 separate matters or 1 campaign?
- Paracale-001: active, or maintenance mode?
- Don Qi role (client vs co-principal).
- Recovery vs. settlement posture per transferee (the 20-transferee campaign).
- **Live layer — build the tier-4 OCR remediation path?** (§6B W1) — the only lever that *earns*
  provenance, so the only way Paracale docs reach fully-connected (0/388 today). Pipeline-desk build.
- **Ratify `corpus_backfill_state.embedded` as the canonical "embedded" source?** (§6B W3) — small
  embedder change; removes the 3-doc gate/embedder divergence.
- **Remediation priority — MWK before Paracale?** (§6B W5) — bears on "Paracale active vs maintenance" above.
- ~~Product versioning kickoff~~ — **DECIDED 2026-06-30: develop the product relentlessly.** A
  dedicated commercialization subagent fleet now exists at `.claude/agents/` (product-hardener ·
  ship-packager · revenue-engineer · truth-qa-gate) driving toward a retainer-ready workspace for the
  proof clients (MWK-001 + Paracale-001), §4A money model. Still open: capital strategy.

## 8. Slip / change log

- **2026-07-07** — **LIVE-LAYER master plan set (§6B); role split.** The live layer (corpus
  connectivity / intake / remediation — *mutates the corpus*) is separated from the ontology layer
  (`ONTOLOGY.md` / validators — fed by directive, never self-edited). "Connected" is now the deploy_712
  `connect-verify` 5 signals (text · provenance · `ocr_quality` · `document_type` · embedded); baseline
  **86/1579** corpus, **0/388** Paracale. Binding constraint = **provenance, earned not faked**: 4/5 signals
  are deterministic + automated (sweep §3.5 + `com.landtek.embed`, deploy_715/716); only provenance + text
  are earned by a real engine read → flagship = the tier-4 remediation path (W1). The 2026-07-06 backlog run
  already moved Paracale embed **102→385**, quality **301→388**, corpus provenance **0→86**. §5A "OCR attack"
  superseded; ontology changes handed off by directive.
- **2026-07-04** — **GEOMETRY-INTAKE ("strip the plot info from the maps/titles") built (deploy_684/685/687).** Goal: mine plottable geometry out of the corpus → `parcels` shapes → later georeference into `map_parcels`. `scripts/strip_plot_info.py` (deploy_684, fixed _685) sweeps titles/plans for metes-and-bounds → `survey_geometry` → `parcels`, closure-error quality-gated, never fabricates. **KEY FINDING (MWK-001 run): the plot info IS in the corpus but standard OCR garbled it beyond use — 0 usable of 54 candidate docs** (the 3 that parse have km-scale closure; computed 489 ha vs 0.9 ha registered). Text-only extraction yields nothing plottable. **Real path = vision re-OCR** (reocr_gemini's prompt preserves bearings exactly → the $0 text-parse then works). Blocked only by **Gemini free-tier 429** (confirmed exhausted today). Jonathan's call: **drip-drain on free tier, Balane chain first** → deploy_687: `geometry_priority` queue (Balane chain parents seeded — subdivision plan + T-4497→T-32917→T-52540; Balane's own lot T-079-2021002126 NOT in corpus, awaits RD CTC) + `scripts/geometry_pipeline.py` (reocr→strip per doc, `--max 6`/run, stops clean on QuotaExhausted) + daily `landtek-geometry-drip.timer`. Reuses `reocr_gemini.py`'s Drive-fetch + key×model ladder + resumable log (no rebuild). Tie-lines (BLLM anchors) confirmed present → absolute georeference → `survey`-tier `map_parcels` feasible once calls are clean. Escalation open: paid Gemini billing = whole chain in one pass.
- **2026-07-04** — **MAPPING subsystem v1 SHIPPED + APPLIED (deploy_682 → corrected by deploy_683).** New client-facing capability: a client opens a token-gated mobile map, sees their parcel drawn on Esri satellite, and taps "Locate me" to learn whether they're standing **inside their boundary** and the distance to the nearest edge (device GPS + on-device point-in-polygon). Resolves the old "geospatial needs a separate PostGIS DB" blocker (§4A) by storing geometry as **GeoJSON in JSONB** on the production DB with area/centroid/overlap math in pure Python. **Correction:** deploy_682 named the table `parcels` and collided with a pre-existing (empty, but code-referenced) survey-extraction table of the same name (`scripts/parcels.py` — relative local-meter WKT from metes-and-bounds); `CREATE TABLE IF NOT EXISTS` silently skipped and the apply half-failed. deploy_683 renames the client-map layer to **`map_parcels`** / **`map_parcels_client`** and documents the two as distinct complementary layers (relative survey shape vs. absolute placed parcel; bridge = tie-point georeference). Pieces: `migrations/deploy_683_map_parcels.sql` (MWK-BALANE seeded UNPLOTTED — no fabricated coords), `leo_tools/geo_math.py`, `leo_tools/mapping.py` (blueprint: `/ops/map` draw tool [Leaflet-Geoman, basic-auth] + `/client/<token>/map` [reuses `client_access` token = one client] + GeoJSON feeds + save), `scripts/mapping_agent.py` (`audit` plot-vs-title area, `overlaps` cross-owner encroachment leads), `.claude/agents/mapping-agent.md`, `systemd/landtek-parcel-audit.{service,timer}` (daily). Accuracy is a provenance tier: `rough`→`survey`→`ortho`; every non-ortho parcel renders an "APPROXIMATE — not a survey" banner. **Switch held OFF** (internal proof on MWK-BALANE only). **Next:** plot MWK-BALANE at `/ops/map/draw?parcel=MWK-BALANE`. Local-tested (geo_math area/centroid exact on a 50m square; both HTML templates + module compile clean).
- **2026-07-03** — **DEPENDABILITY harness + per-client ship gate built (product-hardener, $0, NOT yet committed/deployed — change set handed to operator).** Replaces the reactive whack-a-mole (fabricated Aug-1 date / phantom overdue / garbled label fixed one at a time) with a STANDING, systematic measurement over the *exact facts a client SEES* on the live portal + matter-detail render. New: `scripts/client_dependability.py` (audits Correct/Complete/Stable → a 0–100 Dependability Score + SHIP gate = score ≥90 AND zero correctness fails; CORRECT weighted 60, one fabricated/leak fact tanks the score to 0), `client_dependability` results table, `/ops/dependability` cockpit page (route + nav added to `ops_dashboard.py`; tested in isolation, LIVE service untouched), `systemd/landtek-dependability.{service,timer}` (6h; built, NOT enabled). Reuses (no rebuild) `client_portal.render_*`/guards, `deadlines.py` freshness, `knowledge_coverage` verified-fact ratio. **Measured NOW (honest, low): MWK-001 = 25.8/100 (25 correctness FAILs), Paracale-001 = 14.9/100 (10 FAILs) — BOTH FAIL the gate, 0/2 handoff-ready.** Top defect the harness surfaced = a **live client-separation breach**: docs whose `case_file` is one client are linked via `document_matter_links` to another client's matters and RENDER on the client page (PAR-CAPACUAN shows 84 downloadable docs incl. MWK + NIBDC files; reverse too). Also flagged: **no timer runs `deadlines.py --write`**, so `surfaced_deadlines` (the client-facing dates) is only as fresh as the last manual run — silent-staleness risk (NB: `landtek-proactive.timer` = a *separate* Telegram-push job `proactive_deadlines.py`, disabled — NOT the data refresher; agent mis-identified it, corrected here); `logrotate.service` in `systemctl --failed`. Separation fix spun off as its own task (data-cleanup + render guard). Next: fix separation → re-score; add a `deadlines.py --write` refresh timer; then completeness (only 9/15 MWK + 0/4 Paracale action-matters are dated; verified-fact cov 25%/14%).
- **2026-06-30** — **commercialization fleet established (`.claude/agents/`).** Operator directive:
  "develop the product relentlessly" → ship a retainer-ready workspace + bring in money (§4A: PHP
  15–50k/mo, >85% margin, proof clients MWK-001 + Paracale-001). Authored 4 Claude Code subagents,
  $0 to exist, each baking in the non-negotiables (no hallucination · `_safe` views · provenance ·
  client separation · S14 · stay-cheap/sim-stays-dead · git discipline · no financial execution):
  **product-hardener** (reliability of what exists), **ship-packager** (client-visible surface),
  **revenue-engineer** (pricing + >85% margin proof + cost governance), **truth-qa-gate** (adversarial
  pre-ship gate). Orchestration + map in `.claude/agents/README.md`. Resolves the §7 product-versioning
  decision. Net-new role definitions — NOT a rebuild of the resident agent fleet or case automation.
- **2026-05-13** — pre-trial held; case advanced to motion practice (phase change, not a slip).
- **2026-06-02** — mediation held; May-2026 docs had targeted a "v1.0 by June 2" cut — not cut; north star reset to **Aug 12** (trial testimony).
- **2026-06-13** — droplet upgraded 1 GB→2 GB RAM / 33→67 GB / +2 GB swap ($16/mo); load 1.8→0.5, freezes resolved. VPS git resynced **381→423** (had drifted 42 commits behind, phantom-dirty — running code already matched origin, only HEAD was stale). `notifications/pending.txt` gitignored to stop the recurring dirty-tree-blocks-pull friction that caused the drift.
- **2026-06-13** — **stack-completion build (deploys 427–430), architecture-first / zero API spend:** cost-metering bridge (`anthropic_spend_bridge.py` → `llm_spend`; validated 25 execs = $2.82) + `/ops/spend` cockpit + `leo_qa_runner` v2-stub fix + `activate_stack.sh` runbook + carried-over blockers re-verified (mostly de-haunted). Synthetic loops removed from `fullstack_engineer` CRITICAL so SRE no longer auto-resurrects the paused sim. The stack is built + inert; it flips on via `activate_stack.sh` once credits are topped.
- **2026-06-14** — **OCR-quality remediation (deploys 442–448), creditless re: Anthropic (Gemini free-tier):** "has text" ≠ readable — the mother title T-4497 and the Llamanzares docs had garbage OCR (`Transter Certiiicate of Titie`) that passed length checks. Built: `ocr_quality.py` (zero-API dictionary-hit-rate scorer → `ocr_quality` table; corpus triage = **375/1075 flagged, 92% already clean** so we don't waste quota re-OCRing good text) + `reocr_gemini.py --sweep` (rate-limited, resumable via `reocr_log`, **key×model ladder** primary/​fallback key × 2.5/2.0-flash, page-capped, prioritizes text-bearing legal docs; Drive-fetches docs whose bytes aren't local — T-4497 is Drive-only). **Proven:** doc 39 (T-4497) garbage → clean `LAND REGISTRATION AUTHORITY / MEMORANDUM OF ENCUMBRANCES / P.E. 331 … March 12, 1937`; doc 1106 re-score 0.18→0.66. **Blocked on:** (a) Gemini free-tier daily quota exhausted as of build — drains as it resets; (b) `landtek-reocr-sweep.timer` built + committed but **not enabled** (standing-service auth needed — enable to auto-drain ~400/day over ~3–5 days, or add GPT-4o-mini rung to drain now for ~$1–2 OpenAI, non-Anthropic).
- **2026-06-14** — **Grounded Matter Engine — spine + evidence matrices (deploys 450–452), creditless:** the forward-thinking target = an engine that generates a per-matter *web of facts* grounded in current law + correspondences, kept up-to-the-minute. Built the source-agnostic spine: `matter_engine_schema.sql` (`legal_authorities` versioned law-currency w/ `effective_date`/`as_of_checked`/`superseded_by` + `matter_authorities` + `matter_facts`/`fact_edges` graph + `matter_state`) and `matter_freshness.py` (event-driven staleness — fingerprints each matter's live inputs, flags STALE + says what changed; **live on all 33 matters**). Then `matter_elements.py` — PH-law element frameworks per matter type (accion reinvindicatoria / RA 11032 / just compensation / estate admin / title-chain / criminal / guardianship / generic) instantiated as `matter_elements` rows + each matter's already-linked docs mapped to each element = **element→evidence→gap matrix for all 33 matters (63 gaps)**. **Law source = AnyCase (anycase.ai)** — daily-synced PH jurisprudence/statutes, login-only (no API) → ingest via authenticated browser pull or PDF export. **v1 limit:** keyword mapping over-reports coverage on doc-heavy matters (CV26360/349, ESTATE/593 show 0 gaps); precise have/partial + best-exhibit + authority grounding need the AnyCase + LLM-verification layers (credit-gated). **Engine also surfaced linking gaps:** PAR-VITO-CRUZ / PAR-COMPLAINT-ACE / PAR-TCT1616 show 0 linked docs despite docs existing.
- **2026-06-14** — **Grounded Matter Engine — agentic layers: offensive + strategy + revenue (deploys 454–458), creditless.** The principle that resolves "agentic but cheap": *preparation is deterministic rules ($0) and runs constantly; credits are spent only when the operator fires a chosen move.* Built: (1) `play_engine.py` — per-matter ranked OFFENSIVE queue (ready|blocked|watching) computed from the evidence matrix + deadlines + posture; gated by matter status; `matter_plays` + war room (27 ready moves). (2) `strategy_engine.py` — the client-strategy layer: operator-confirmed **north-star** (Keesey = recover+consolidate the T-4497 estate), `matter_objectives` leverage scoring, and **`keystones`** (the de la Fuente void-SPA finding cascades CV26360 → TCT4497 + ESTATE + 20 transferees); campaign board re-ranks the war room by advancement-of-the-goal, leading with the keystone. (3) `revenue_engine.py` — **second north-star (monetize ASAP)**: `property_assets` inventory, each opportunity decomposed into a first-principles precondition chain per mode (sale/lease/develop/mineral); the board surfaces the BROKEN link + unblock move, fastest-money-first, and cross-links to litigation (Keesey parcels sale gated on CV26360; 23-ha mineral gated on PAR-CASE-88750). **Needs operator input** to be real: the commercial state of earn-now assets (possession/occupancy/rentable) + the existing-sales pipeline (e.g. doc 165 "Yanto partial payment") — the board flags these as "needs your input."
- **2026-06-14** — **Awareness is an ACCRETION problem, not a budget problem (deploys 461–465). LOAD-BEARING — do not regress.** Operator challenge: "we've had LLM budgets before and the system is still clueless." Diagnosis (data-backed): prior spend went to the **simulator** (2,427 `leo_qa_sim_payloads`, ~$40/day of ephemeral chat-QA) + shallow indexing (5,054 entities, 833 RAG chunks) — never to durable, reconciled knowledge. Result: 6 claims, **0 matter-facts**, **2/77 titles** with a known status. Budget with no accretion target = clueless forever. **Corrections now in place:** (1) `comprehend.py` — reads each doc → writes a durable grounded fact (title status / value / possession) with excerpt + confidence; high-conf overwrites defaults, low-conf flags the operator (human = exception-handler). (2) `knowledge_coverage.py` — **the awareness meter**: scores what the system should know vs. actually knows → **13.0% as of build**; every LLM dollar must move this number or it's waste. (3) metering (`anthropic_spend_bridge` records spend by source) + the simulator stays OFF. **Standing rule: spend budget only where it moves the awareness score; the sim is the money pit, keep it dead.** Comprehension runs on Gemini free-tier as quota resets (shares the re-OCR budget) or instantly with Anthropic credits; quality scales with OCR (the re-OCR sweep feeds it).
- **2026-06-14** — **Two-Cowork alignment + browser-transport ingestion (deploys 467–471).** Awareness is **live-measured** — do NOT quote a fixed number; run `knowledge_coverage.py` (was 13% at the meter's birth, **34.5% after the free `harvest_facts` regex pass**, `awareness_log` tracks it). Cost model corrected: the heavy thinking runs on the **Claude Cowork subscription** (this session — proved it by reading Balane's title from the local Drive folder and finding a US/Sacramento registrant-address anomaly on T-079-2021002127), not the depleted metered API. **The corpus is local on the Mac** (`~/Library/CloudStorage/GoogleDrive-jonathan@hayuma.org/My Drive/LANDTEK`, ~950 files) → Claude vision OCRs it directly. **Transport-agnostic ingestion contract** (DB read → any LLM → DB write): `comprehend_browser_adapter.py` (text→facts via Gemini Advanced browser) + `ocr_browser_adapter.py` (image→text via Gemini Advanced browser; `--next-ocr`/`--write-ocr`, re-scores quality, 341 pending). Operator's choice: **Gemini Advanced (browser) for OCR** (separate sub quota, strong at OCR). **Multi-agent rule:** two Cowork windows share this working tree — the session with VPS+local+browser access is the EXECUTOR (commits/deploys/runs); the sandboxed one is the DESIGNER (drafts files, executor rescues+commits). Only one commits; `pull --rebase` before every commit.
- **2026-06-15** — **OCR attack + T-4497 chain documented end-to-end (Cowork subscription, $0 API).** The hybrid color-correction OCR solution is complete: `ocr_preprocess.py` (gray default — blue over-thins faded ink) → **tile-crop dense pages** (Claude vision downsamples full pages; cropping each entry-block = citation-grade) → Claude vision → Principle-9 marking → `ocr_browser_adapter --write-ocr` → comprehend. Validated across doc types (clean SPA 246, faded title 87, 13-pg bundle 97, dense mother-title MoE 25). **The T-4497 chain is now on verified sources, root to tip:** OCT-111 (1937 A.L. Amen lease) → 1941 Partition (Mary Worrick-Keesey **+ Dolores G. Ortega** — new co-owner) → 1988 EJS (3 heirs, Consul Que) → **1992 SPA "to NEGOTIATE for the sale"** (Consul Munda; the SJ kill-shot, now confirmed in the RD's own title annotation = 4th source) → 1993 Subdivision (de la Fuente names transferees: Onrubio, Iligan, de Leon, King, Espinosa, Yasis) → 2005 revocation (published 2020) → **2010 subdivision into 24 lots (PSD-221861) creating the 079- series** the transferees hold → 2016 Balane deed (void 3 ways: negotiate≠sell, ultra vires of Llamanzares-only scope, post-revocation; + "Series of 2014" notarial irregularity). **33 matter_facts, 29 verified** (created_by='cowork_comprehend'); `title_chain` extended with OCT-111→T-4497 + T-4497→079-2010000663. **Remaining (systematic):** designer wires `--tile`; tile intermediate MoE pages 4-12; populate full `title_chain` + transferee→title links; continue the 341-doc OCR backlog.
- **2026-06-15** — **RAG retrieval is LIVE — the corpus is queryable (competent RAG achieved).** Was the critical gap: 9/1055 docs embedded (Gemini quota died). `rag_embed_local.py` embeds the WHOLE corpus with a LOCAL model (fastembed `bge-small-en-v1.5`, 384-dim ONNX — no GPU/quota/credits, $0) on the Mac, shipping vectors to VPS `rag_local` (vector(384)+HNSW) over ssh COPY. **Now 1055/1055 docs, 9,451 chunks.** Retrieval validated on case queries (EJS Keesey 1988 → TCT-4503 MoE; "Balane owner's duplicate" → the Complaint + Jonathan's Judicial Affidavit) — and it matches by MEANING, so it works even through garbage OCR. **Remaining for quality:** the 372 garbage-OCR docs are embedded but their retrieved snippets are noisy → the OCR attack cleans them (then re-embed on text-change, a `--refresh` mode TODO); 54 docs are physical-only (need scanning, not OCR). `rag_local` (local 384-dim) supersedes the abandoned `document_chunks` (Gemini 3072-dim, 9 docs).
- **2026-06-15** — **Inocalla / Keesey de-conflation (operator directive "Allan Inocalla should not be conflated with MWK"), $0 DB work.** Established the **Allan Inocalla ≠ MWK invariant** (parallel to MMK≠MWK): Allan appears in MWK-001 *only* as the disinterested-person AFFIANT (witness) in Patricia Ann Keesey Zschoche's delayed-birth-registration packet (docs 1155/1157) — never as a party. Corrected the genuine mis-files: docs **513, 525** (Inocalla family litigation — Jesus/Herbert/Cipriana v. de Leon Inocalla; Vicente Jr. testimony) MWK-001 → **Paracale-001**; doc **803** (Jonathan's NIBDC due-diligence authorization) → **NIBDC-001**; their stale MWK `document_matter_links` deleted. Verified clean: **zero Inocalla entities tagged on any MWK-001 doc**, no Inocalla in MWK matter party-lists; the omnibus bible (958) is 91-Keesey-vs-2-Inocalla and its Inocalla mention is a doc-inventory line, not conflation (stays MWK). Entity hygiene: merged the 3 duplicate Allan entities (7983 + 8091 + 8147, all FK-isolated to doc_entities) → one canonical **"Allan Villafria Inocalla"** (id 7983, 37 links, spellings as aliases) and **dropped the "Datu Shishir" alias** — this *reconciles the DB to deploy_258's documented canon* (`_clients.py` already records 8091/8147→7983 and holds Shishir 8708 separate "pending clarification"; the DB had drifted because auto-extraction re-spawned the variants). **Also registered NIBDC** in `case_theories/_clients.py` (was a DB client but unregistered → the stale `case_file_domain` truth test flagged it). Invariant recorded in `matter_facts`. **FLAGGED next granularity rung:** the broader Inocalla-family entity-resolution (~29 name-variants → ~12 real people, incl. re-applying the Shishir 8062/8776→8708 consolidation that also drifted) wants a systematic verified dedup pass, not piecemeal.
- **2026-06-15** — **Cross-client anti-conflation ENGINE (deploy_485) — "this can never happen again."** Operator verdict on the Inocalla-under-MWK mis-file + the re-spawning duplicate entities: *"it's the sign of a dumb system; if names are in multiple clients' corpuses the system must be intelligent enough to understand WHY."* Built `scripts/cross_client_sentinel.py` — a deterministic ($0, no LLM) intelligence layer that distinguishes the three real failure modes: **(1) legitimate overlap** — role-aware classification of every person/org spanning >1 client (places/laws/dates are typed and ignored; a person *defining* in ONE client but only incidental — witness/notary/appraiser/agent/mentioned — elsewhere is EXPECTED, e.g. Virgilio Tuazon the appraiser-for-both, or Jonathan the operator-agent; defining in >1 client = CONFLATION_RISK → human); **(2) mis-file** — text scan flags a doc naming ≥2 distinct *parties* of another real client and zero of its own (the 513/525 pattern; officials like the Register of Deeds and the operator's OCR-garbled duplicates are excluded so they don't masquerade as parties); **(3) drift** — the deploy_258 entity consolidation had silently drifted back apart (8320/8062/8776/8158 live again + new exact-dups Cesar 9036, Gloria 3716). Root cause: the canon lived in a code *comment*. Now it's **data** — `_clients.CANON_ALIAS_MERGES` — re-applied idempotently by `--apply-canon` (FK-complete merge across 12 ref tables; uses the audited `truth_override` to write hard-locked keystones). Applied all 6 merges. **Enforcement so it can't rot:** `truth_tests/test_cross_client_integrity.py` (3 assertions: canon-applied / no-cross-principal / no-misfile) — full suite now **71 green**; plus `systemd/landtek-cross-client.{service,timer}` (daily 03:30 UTC: re-apply canon + refresh `cross_client_flags`), **committed but NOT yet enabled** (operator action — `systemctl enable --now landtek-cross-client.timer`). Also re-filed doc 540 (Mary Worrick Keesey tax dec) Paracale→MWK. **HONEST GAP (flagged, not solved):** the corpus has wholesale entity fragmentation from OCR — 30+ garbled "Zschoche" spellings, ~29 Inocalla-family variants for ~12 people — which needs a dedicated fuzzy/LLM entity-resolution pass; the sentinel is robust *without* it (name-aware operator allowlist) but real dedup is the next rung. The single-owner mis-file (one party, like a lone tax dec) is below the ≥2-party text threshold — caught only manually for now.
- **2026-06-16** — **Status correction: Botor engaged, guardianship petition filed.** MWK-GUARDIANSHIP is no longer in draft stage — Atty. Adan Botor is counsel of record (engaged), petition is filed at RTC Daet. Updated MASTER_PLAN §1 + §7 to reflect. This is the canonical truth going forward; **never ask the operator about Botor's status again** — it's in the MASTER_PLAN §1 as ✅ verified.
- **2026-06-16** — **Entity resolution: collapsed OCR-variant fragmentation (deploy_486), $0.** Closes the "honest gap" from deploy_485. Built `scripts/entity_resolve.py` — deterministic ($0, no LLM) variant clustering by a normalized name key (lowercase, deaccent, strip honorifics atty/datu/dr/engr/rea, drop single-letter initials, but **KEEP** generational suffixes jr/sr/iii so father/son/namesake stay distinct; "heirs"/"of" kept so the *estate-collective* "Heirs of Mary Worrick Keesey" never collapses into the *person* #25). Conservative by design: **AUTO-merge only exact-normalized-key within ONE real client** (provably same person) — **applied 132 merges, person entities 1024→892**; cross-client same-name (namesake risk) is HELD, single-token bare names are too weak to act on, and **231 fuzzy OCR-typo pairs** (e.g. "Paricia"→"Patricia", "Edgard"→"Edgardo Santiago") are written to `entity_merge_proposals` (status=pending) for one-command `--accept`, NOT auto-merged (over-merging two distinct people is the same dumb error in reverse). Reuses the sentinel's FK-complete `merge_entities` (also fixed it to re-point the self-referential `entities.canonical_id`). Truth suite stays **71 green**; sentinel still 0 drift / 0 mis-file post-merge. **Remaining:** review/accept the fuzzy proposals (operator threshold call); the daily cross-client unit can additionally run `entity_resolve --apply-auto` to keep re-spawns collapsed.
- **2026-06-16** — **Entity resolution: reviewed-accept pass on fuzzy proposals (deploy_488), $0.** `entity_resolve.py --review-accept [--apply]` classifies the 231 pending fuzzy pairs: auto-accepts only pure same-structure OCR typos (Levenshtein ≤2, no gender a/o flip), and HOLDS anything riskier — generational suffix diffs (father/son), subset/token-drops ("Carlos" ⊂ "Carlos Vargas"; "Joseph Guy" ⊂ "Guy Joseph Hopp" — surname dropped), and edit-distance-3. Apply uses union-find to collapse accept-chains to one survivor per cluster and **refuses any cluster spanning >1 keystone** (a fuzzy chain must never fuse two distinct keystone people). **Merged 100 variants across 55 clusters; 72 held for human review** (56 subset, 13 edit-3, 3 generational — query `entity_merge_proposals WHERE status='held'`; accept one with `--accept <survivor> <alias>`). **Person entities this session: 1024 → 792** (232 OCR duplicates collapsed). Truth suite **71 green**, sentinel 0 drift / 0 mis-file throughout.
- **2026-06-16** — **`landtek-cross-client.timer` ENABLED** (operator authorized). Daily 03:30 UTC self-heal is now LIVE: `entity_resolve --apply-auto` (collapse re-spawned exact-dup variants) → `cross_client_sentinel --apply-canon` (re-merge documented aliases) → `--report --log` (refresh `cross_client_flags`). Test-run succeeded (exit 0). Future sessions: do NOT re-enable; check `systemctl status landtek-cross-client.timer`.
- **2026-06-16** — **SYSTEM GROUNDED BRIEF — the synthesis/facts layer (deploy_490), $0 deterministic.** Operator: the system was fragmented (MASTER_PLAN manual+stale, DB facts un-synthesized, engines running without unified grounded context). Built `scripts/grounded_brief.py` → generates `SYSTEM_BRIEF.md`: a machine-synthesized FACTS mirror of the DB (`matters` + `matter_facts` verified-only + `keystones` + `matter_state`). **Deliberate design (per design review):** FACTS only — intent/north-star stay MANUAL in this file (the brief POINTS here, never duplicates); fixed operating-rules preamble + generated body (generator only writes the body); confidence is law — only `provenance='verified'` facts (31) and keystone cascades (1: the de la Fuente void-SPA, grounded in its verified docs) are ASSERTED, the 5183 inferred shown as COUNTS only, candidate cascades (shared-title overlap) in an UNKNOWN section for human promotion — **never** auto-promoted (overlap ≠ cascade, the cross-client lesson). Rides `landtek-cross-client.timer` (regenerates after the daily self-heal). **`SYSTEM_BRIEF.md` is gitignored** — a tracked file rewritten nightly would dirty the VPS tree and break the multi-agent pull protocol (cf. `notifications/pending.txt`); it lives where it's consumed (VPS), regenerate with `python3 scripts/grounded_brief.py --write`. **Step 2 (pending):** wire the brief as grounding context into the LLM entry points (Leo systemMessage, comprehend, play/memo generation) so generated output can't drift from verified ground truth.
- **2026-06-16** — **Constitution consolidation — two parallel builds merged into one (deploy_491).** Collision: the designer window built `constitution_generator.py` + `SYSTEM_CONSTITUTION.md` in parallel with my deployed `grounded_brief.py` + `SYSTEM_BRIEF.md` — the parallel-doc sprawl CLAUDE.md forbids. Operator decided: keep mine (data-driven, grounded, runs — the designer's draft had a syntax error + hardcoded/ungrounded cites like Art. 1874 / "Bautista-Spille" where verified facts say Art. 1878 / *Yoshizaki*), harvest its better FRAMING, name it `SYSTEM_CONSTITUTION.md`. Done: renamed `grounded_brief.py` → `constitution_generator.py`; output → `SYSTEM_CONSTITUTION.md` (gitignored, regenerated on the daily timer); added a **NORTH STARS** section sourced from `client_goals` (north_star + active sub-goals) — *data, not the designer's hardcoded dict*; retired the designer's duplicate. One canonical fact-substrate now.
- **2026-06-16** — **Leo discernment architecture — think, not infer (deploy_492), $0.** Operator mandate: "an architecture that makes LEO think and not infer; with facts he must be discerning; anything less is a waste." Built the enforcement layer (not just prompting): **`scripts/leo_answer_gate.py`** — a deterministic ($0, no LLM) gate run on every Leo reply BEFORE it ships: (1) **citation resolution** — every `doc:N` cited must resolve to a real `documents` row (fabricated cite → FAIL; real-but-unverified → warn); (2) **cascade grounding** — an asserted cascade with no keystone, or a sweeping consequence with zero citations, → FAIL (the top legal-hallucination class); (3) **uncited-claim heuristic** — fact-signal sentences without a cite → warn. Tested: grounded reply PASS, fabricated doc:99999 FAIL, uncited "all 20 transferees lose titles" FAIL. Paired with **`leo_discernment_protocol.md`** — the systemMessage discipline (retrieve-then-reason from the Constitution; cite every fact; tag verified/inferred/unknown; cascades only from keystones; "not in the record" beats a fluent guess). **Architecture status:** substrate (SYSTEM_CONSTITUTION) ✅, verify-after gate ✅ built+tested, retrieve-before protocol ✅ authored. **Wiring pending (touches live n8n + leo-tools, and Leo can't run until credits):** prepend the protocol to the AI-Agent systemMessage + inject the Constitution; add a `get_verified_facts` leo-tools endpoint; insert the gate as a post-generation node (fail → block/regenerate). Apply at activation.
- **2026-06-20** — **PHASE 0 COMPLETE — provenance write-gate (the architecture fix), $0.** Operator caught the system (and me) writing an inference (Engr. Erwin Balane's role) as a *verified* fact — proving the knowledge layer had **no write discipline**: anything could INSERT a row stamped 'verified'/'operator' with nothing checking it traced to a source. Root cause: provenance was a *label*, not a *constraint*; we'd built an output-gate for Leo but no **write-gate** for the knowledge layer, so corruption entered on write. **Fix (`migrations/apply_provenance_gate.py`):** three canonical tiers — **verified** (document-proven: resolving source doc + quoted excerpt; the ONLY tier surfaced as fact) · **operator** (Jonathan's assertion; trustworthy as his word, NOT doc-proven; promotable only by a source-read) · **inferred** (derived; never surfaced as fact). Enforced by BEFORE INSERT/UPDATE **triggers** on `matter_facts` / `matter_parties` / `matter_causes` — a row cannot be stored 'verified' without a resolving citation (smoke-tested: uncited verified write rejected). **Re-tiered the mislabelled rows:** 2 operator-facts + 2 parties + 1 cause (incl. my hand-fed decipher) → operator; matter_facts now **32 verified (all doc-cited) / 2 operator / 5183 inferred**. **Standing audit** `truth_tests/test_provenance_integrity.py` (4 assertions; suite now **75 green**) fails any future deploy that reintroduces an uncited verified row. Writer scripts (`load_issue_spine`, `decipher_matter`) fixed to emit the honest tier. **This unblocks Phases 1–4** — nothing downstream was trustworthy until inference could no longer masquerade as fact.
- **2026-06-20** — **PHASE 1 source-reads (the comprehension that produces VERIFIED facts), $0 via Cowork.** `scripts/source_read_facts.py` writes facts READ from the OCR'd source into gate-passing verified rows (source_kind=doc + resolving id + quoted span). Cowork (this Claude) is the comprehension engine — $0 to the metered account. **Read + extracted:** doc 419 (filenamed 'Exhibit I Complaint' but is actually Patricia's **Affidavit of Adverse Claim** + a garbled T-4497 copy — mislabel corrected) → 7 verified void-chain facts; doc 384 (filenamed 'Complaint Affidavit (3)' but is the **ARTA-0747 Complaint-Affidavit vs Mayor Pajarillo** — mislabel corrected) → 6 verified facts; doc 683 (CV-6839 Commissioner's valuation, Yago 18-Nov-2015) → 1. **+14 verified facts (32→46), all cited; 0 gate-rejected.** **Conflation resolved by reading the source:** Engr. Erwin Balane = the **Municipal Engineer** who refused records absent an SPA in ARTA-0747 — distinct from his CV-26360 transfer/subdivision role (the discipline stopped a merge). **HONEST GAPS, flagged not faked:** (a) the **₱20M** figure lives in OCR-corrupted valuation tables → stays **operator**-tier until a re-OCR or the court decision; (b) the **real CV-26360 complaint** is not identified in the corpus (419 & 384 were both mislabeled to other docs) → 26-360's full defendant roster + Erwin's exact 26-360 cause stay **operator**-tier until the actual pleading is found/read. Phase 1's remainder is bounded by **OCR quality + document identification** (the re-OCR track) — not by more asserting.
- **2026-06-20** — **PHASE 1 unblocked — the legible truth is in EMAIL (deploy_506).** Operator: "they are all legible and most have been sent through my email." Correct — the degraded scans in `documents` (419/384/683) were decoys; the real operative pleadings are **email attachments** in `gmail_messages`. Found **doc 781 = 'Latest Draft Complaint - Zschoche v. Balane, et al.'** (the REAL CV-26360 complaint, from the Barandon thread; 41K legible chars). **CV-26360 now fully deciphered FROM SOURCE, all verified + cited to doc 781:** 6 defendants — Sps. Efren & Gloria Balane, **Sps. Jomil & Princess Balane Torralba (the present illegal possessors — the system never had them)**, and **Engr. Erwin Balane (Municipal Engineer, impleaded for using his official capacity to validate/give effect to the illegal sale — the operator's transfer/subdivision correction, now exact and cited)** — plus 4 verified causes (accion reivindicatoria, nullity of the 2016 deed, cancellation of TCT 079-2021002126 + ARP, accounting & damages) and foundational facts (subject = 2,587 sqm of T-52540, assessed Php257,670; the verbatim 'negotiate not sell' SPA language). Verified facts **46→48**; suite **75 green**; gate held (0 rejected). **The remaining matters' operative pleadings are also in email** (the ARTA complaint-affidavits, the Judicial/SJ affidavits, the 'CARP Execution Summary' for the ₱20M) — Phase 1 can be completed by reading them, no OCR blocker after all.
- **2026-06-20** — **STANDING VERIFIABLE-CORPUS LOOP (deploy_507).** `scripts/verify_loop.py` — a $0, deterministic daily loop that (1) SCOURS the knowledge layer (matter_facts/parties/causes) for every not-yet-verified row and enqueues it into the shared `verification_queue` (idempotent; 5,185 candidates enqueued, joining the 81 title-graph rows already there); (2) POINTS at the highest-value LEGIBLE source docs to read next (email attachments + good-OCR, matter-linked, ranked — 296 awaiting a read); (3) MEASURES verifiable-corpus coverage so the number visibly climbs. Wired to `landtek-verify.timer` (daily 03:00 UTC, now enabled). It never writes 'verified' itself — the comprehension read (Cowork now / an LLM reader once activated) + the provenance gate do that. **Continued source-reading (+8 verified):** CV-6839 finally identified from source (doc 376, Yuzon memo) = *Heirs of Mary Worrick Keesey v. DAR + LandBank* just-compensation/agrarian case — preliminary payments ran through the same **Cesar de la Fuente** and the same **15-Aug-2005 SPA / Ida Buenaventura** thread as CV-26360 (a cross-matter link found from source, not guessed); ARTA-0747 outcome read from doc 967 (the ARTA Resolution = referral to CART, and the respondent **Mayor Pajarillo is himself the CART Chairperson** = Termination-of-Referral ground (a)). Coverage baseline 1.3% (66/5251 knowledge rows verified). **ARCHITECTURE FLAG (open):** `verification_ladder.py` auto-promotes entities to 'verified' by 3-mention **corroboration** — this contradicts the gate (verified = cited doc + excerpt). Its timer was already disabled; it is now **quarantined** (the daily verify job points at verify_loop.py instead) pending a relabel of corroboration-grade rows to a distinct `corroborated` tier so 'verified' means document-proven system-wide.
- **2026-06-20** — **CONTINUOUS AUTONOMOUS VERIFICATION (deploy_510)** — the answer to "why nightly, not continuous?". The loop had two halves and only the cheap *scout* was timed; the *comprehension* half (the actual reading) was me, in-session. Now that half is autonomous + continuous, **$0 on Gemini free-tier (no Anthropic credits)**: **(1) Hardened the gate** (`migrations/harden_excerpt_gate.py` + `excerpt_grounded()`) — a verified row's excerpt must be a **verbatim 35-char-run substring** of its cited doc (OCR-robust: strips punctuation, slides a window). Re-tiered **8 ungrounded** verified→inferred; fabrication smoke-test rejected; suite **76**. This is the **safety floor**: even a hallucinating model cannot land an ungrounded verified fact. **(2) `scripts/verify_worker.py`** — reads the top of verify_loop's worklist via Gemini, extracts atomic claims each with a verbatim quote, and writes through the hardened gate: grounded+confident → **verified**; surfaced-but-ungrounded → **`proposed_facts`** for human review (never auto-verified blind). Attempt-tracked (`verify_worker_log`, 14-day cooldown), paced to free-tier. Wired to **`landtek-verify-worker.timer`** (every 15 min, `--limit 4`, enabled). **Dry-run validated:** doc 351 (CV-6839) → 6 grounded facts at confidence 1.0 (case identity, plaintiff MWK, DAR respondents, the just-compensation petition, property = TCT **T-4494**, the PARAD valuation). Architecture now: **verify_worker** (continuous read+verify) + **verify_loop** (nightly scout/measure) + **hardened gate** (the guarantee). Autonomy is bounded by the gate — the model proposes, the gate disposes.
- **2026-06-21** — **ENTERPRISE ARCHITECTURE ASSESSMENT (`ARCHITECTURE.md`, deploy_514).** Operator asked what we're missing architecturally for enterprise-grade software, and whether in-house (self-hosted, not API) LLMs are needed. Wrote the technical target-architecture + ranked gap analysis (this doc stays subordinate to MASTER_PLAN for direction). **Verdict: the #1 gap is an owned, tiered INFERENCE layer — and it's the rare both-now-and-enterprise move.** It kills the current fuel bottleneck (verification stalled at ~14 docs/day on free-tier quota) AND gives data sovereignty over privileged legal docs AND removes vendor-outage risk. **The hardware already exists and is idle:** the Mac Studio (M2 Max / 32 GB, Tailscale `100.117.118.47`, Ollama installed) can serve ≤32B models; the provenance gate makes a small local model safe ("model proposes, gate disposes"). Other enterprise gaps (HA/PITR, RBAC + access-audit + encryption-at-rest, multi-tenancy, alerting, adjudication UI) are real but **product-grade and premature for a solo wartime op** — deferred with eyes open per "don't build ahead of need." Operator chose **assessment-first**; awaiting decision on (1) approve the in-house inference build, (2) product vs sovereign-personal-platform, (3) 32 GB ceiling vs a future 64–128 GB box.
- **2026-06-20** — **CASE DOSSIERS — each matter's corpus + evidence laid out (deploy_513).** `scripts/case_dossier.py` → `case_dossiers/<MATTER>.md` + `INDEX.md`. Per matter: identity (forum/docket/status/deadline) → **verified parties** → **verified causes** → the **document-proven fact corpus** (every statement with its `doc:` citation + quoted excerpt) → **evidence on file** (each document classified ✅read / 📄legible-unread / ⛔OCR-blocked, with #facts it has yielded) → **coverage & gaps** (verified vs operator-asserted-pending vs inferred; the operator claims still needing a source-read). Reads honestly — only `provenance='verified'` is presented as the corpus. 24 dossiers generated + committed as a snapshot; **regenerated daily** (wired into `landtek-verify.service`) so they track the worker. INDEX at-a-glance: CV-26360 **79 verified**, ARTA-0747 28, CV-6839 17, and the rest now building via the breadth-fair worker. This is the "build upon easily" surface — open a matter's `.md` and the proven case is right there with its citations.
- **2026-06-21** — **INFERENCE TIER 1 LIVE — sovereign in-house inference (deploy_516).** Executed the runbook: pulled **qwen2.5:7b-instruct** on the Mac Studio (Ollama), bound it to `0.0.0.0` so the VPS reaches it over Tailscale (`100.117.118.47:11434`), created the `inference_audit` table, and wired **`verify_worker` to a LOCAL-FIRST backend** (its own grounded-extraction prompt; Gemini fallback; `LANDTEK_INFERENCE_TIER=2` emergency override). **Proven end-to-end:** VPS worker → Mac qwen2.5:7b → hardened gate → matter_facts, all over Tailscale. **267/267 worker facts verbatim-grounded**; `inference_audit` logging tier1 calls; verified facts **141 → 324**; suite **76 green**; 0 gate-rejected. **THE FUEL BOTTLENECK IS GONE** — verification now runs on local, unlimited, $0 inference (no Gemini quota cap), Gemini as fallback. Timer bumped to `--limit 10 --rpm 0`. Built out previously-empty matters (PAR-CV13-131220, PAR-CASE-88750) in the process. **OPEN:** (a) the `0.0.0.0` bind is session-active but **not reboot-persistent** — a LaunchAgent (`mac/com.landtek.ollama-host.plist`) is written but **not loaded** (auto-mode classifier flagged persistent service-exposure → operator decision); the worker gracefully falls back to Gemini if the Mac is unreachable, so nothing breaks. (b) running 7B; **qwen2.5:14b/32b is a one-line quality upgrade** (`ollama pull` + `VERIFY_WORKER_OLLAMA_MODEL`).
- **2026-06-21** — **RESIDENT-AGENT REGISTRY + SUPERVISOR (deploy_517)** — prep for shipping a fully-ready Leo. `scripts/agents.py` = the canonical catalog of every autonomous agent (role / fuel tier / cadence / systemd unit / status) + a `--health` fleet check. **Roster: 7 LIVE** (verify_loop, verify_worker[local-first], case_dossier, cross_client_sentinel, deadlines, build_digest, supervisor) · **6 PLANNED** — gap-fillers `doc_discovery` / `ocr_triage` / `contradiction` / `reconciler` (all det/$0) and output agents `analyst` / `brief_drafter` (local-LLM, now affordable on the in-house tier) · **1 NEEDS-WIRING** (leo: discernment protocol + answer gate + a get_verified_facts endpoint). The supervisor immediately caught a stalled agent — `landtek-digest.timer` was enabled-but-inactive (the daily digest wasn't actually scheduled) → started; fleet now all green. **Ship path:** build the gap-fillers (complete the corpus Leo answers from) → reconciler → output agents → wire Leo.
- **2026-06-21** — **RESIDENT-AGENT FLEET SHIPPED (deploy_517–520)** — operator: "ship all" the planned agents. Registry/supervisor (`scripts/agents.py`, `--health`) + 6 new agents built, tested, registered live: **doc_discovery** (conservative doc→matter linker; caught+reverted a year-false-positive that had mis-linked 65 docs), **contradiction** (proximity date-conflict detector; flags the CV-26360 sale-date 2016-vs-2019), **reconciler** (human-in-loop `proposed_facts` adjudication, gate-checked on accept), **analyst** (local-LLM case theory over the verified corpus — proven on CV-26360's 79 facts, $0 via Ollama; output labeled derived-reasoning, never a verified fact), **brief_drafter** (local-LLM grounded work-product, `[PENDING VERIFICATION]` for gaps), **ocr_triage** (local Tesseract re-OCR to a side table, never overwriting; 2/4 improved, 66 Drive-only queued). **Fleet = 13 live + 1 needs-wiring (Leo).** The daily $0 chain (`landtek-verify.service`) now runs verify_loop → doc_discovery → contradiction → ocr_triage → case_dossier; analyst/brief_drafter/reconciler are on-demand on the in-house tier. **Remaining for a fully-ready Leo:** wire the n8n AI-Agent node to the discernment protocol + `leo_answer_gate` + a `get_verified_facts` path, and point Leo's model at the local Ollama tier (no credits needed) — the live-workflow edit is harness-blocked (operator applies) and is the activation decision.
- **2026-06-21** — **LEO+AGENTS: the filing-cycle agents (deploy_521).** Operator spec (Discovery/Execution/Deadline/Narrative for the Aug-12 cycle), mapped to avoid sprawl: **Deadline = existing `deadlines`** (escalation = pending enhancement), **Narrative = existing `brief_drafter`**. Built NEW: **`filing_monitor`** (Discovery — watches gmail for incoming filings/orders tied to a matter from non-operator senders → `filing_alerts` + tg_send S14 alert; baselined 32 historical filings; **6h timer enabled**) and **`execution_tracker`** (`case_actions` lifecycle ledger planned→drafted→approved→filed→confirmed + stale-watch + `filing_alerts` confirmation cross-ref; on-demand). **HARD LINE: the system NEVER files with the RTC** — no court API and filing is counsel's act ([[landtek-not-a-law-firm]]). The cycle is: court/opponent files → filing_monitor alerts → brief_drafter drafts → operator + counsel approve → **counsel files** → execution_tracker confirms. **Fleet now 15 live + Leo needs-wiring.** Remaining for the spec: deadline-escalation, a Leo `get_verified_facts` data path (both small, buildable), and the Leo n8n node wiring (harness-blocked → operator-applied) + the end-to-end live test.
- **2026-06-21** — **FORUM DESK AGENTS (deploy_522).** `agency_agent.py` — one engine, four forum playbooks (ARTA / CSC / Ombudsman / Civil), per operator request. Each desk knows its forum's deadline clocks, procedure stages, and escalation grounds; it classifies matters to a desk by forum and surfaces per-matter status (verified-fact count + last filing). **ARTA** (RA 11032: 3/7/20-working-day clocks; CART referral + the two Termination-of-Referral grounds) and **Civil** (Rule 15 §5(c) 5-day reply, Rule 22, Rule 35 SJ) playbooks are GROUNDED in corpus docs (384/967/452/1088). **CSC + Ombudsman** desks are scaffolded — no matters yet, periods marked **NEEDS-COUNSEL-VERIFICATION (not fabricated)**. ARTA desk runs 9 live matters, Civil the RTC/MTC cluster. Registry: 4 desks (ARTA/Civil live, CSC/Ombudsman ready). Discipline held: legally load-bearing deadlines are cited or flagged-for-counsel, never guessed.
- **2026-06-21** — **OVERSIGHT-MANDATE DB (deploy_523).** `scripts/load_agency_mandates.py` → `agency_mandates` — 11 oversight/forum bodies (ARTA, CSC, Ombudsman, DILG, OP/Exec-Sec, DOJ/Prosecutor, Sangguniang Bayan, COA, RD/LRA, DAR-DARAB, DENR-MGB), each with its **charter (cited RA / constitutional article)**, mandate, jurisdiction, powers/remedies, how-to-invoke, appeal path, relevance-to-our-matters, and a `verify_note`. Reference-law tier — charters cited; operational specifics (exact disciplining authority, periods) flagged **NEEDS-COUNSEL-VERIFICATION**, not fabricated. This turns the 10-issue inventory into **forum-routing**: which body has jurisdiction over each official's misconduct, what remedy it can grant, how to invoke it. Each official now maps to multiple forums (e.g., Pajarillo → ARTA + Ombudsman + DOJ). Feeds case-building (analyst/brief_drafter/Leo reason WHERE to file + WHAT to seek) and tracking (the forum desks + filing_monitor). **Next:** a forum-router linking each `client_issues` row → candidate bodies + recommended remedy.
- **2026-06-21** — **FLUID CASE BUILDER (deploy_524) — the capstone.** A live case-building interface plugged into the corpus. `forum_router.py` wires each grievance (`client_issues`) → candidate oversight forums (`case_forums`, from the mandate DB) with remedy + rationale (23 routes across 10 issues). New cockpit view **`/ops/cases`** renders each grievance as a live card: candidate forums + remedies + **LIVE corpus support** (verified-fact count, docs, filings) aggregated over the issue's matters — **growing fluidly** as `verify_worker` verifies, `filing_monitor` catches filings, `doc_discovery` links docs. E.g., #7 Pajarillo → ARTA/DOJ/Ombudsman/OP; #6 Balane → 150 verified across ARTA/Civil/Ombudsman; #8 ARTA cluster → 159 verified. Operator promotes a candidate forum to chosen/filed; the forum desks + `execution_tracker` track development. Lightweight — extends the existing Flask cockpit, no new app. Registry: forum_router + case_builder_ui live.
- **2026-06-21** — **FORUM LAW LIBRARY / legal-authority RAG (deploy_525).** `scripts/legal_authority.py` — per-forum verbatim statute/IRR/circular RAG in pgvector, embedded **locally via Ollama `nomic-embed-text` ($0, sovereign)**, so forum agents reason from the ACTUAL law, not a paraphrase. Seeded **ARTA** with **RA 11032 §§9** (3/7/20-working-day processing), **21** (the 8 prohibited acts), **22** (penalties: 6-mo suspension → 2nd offense dismissal + ₱500k–2M fine + 1–6 yr imprisonment), fetched verbatim from lawphil, flagged `verify-vs-official`. Semantic `retrieve(forum, q)` proven (returns §9 for "working days simple transaction", §22 for "penalty repeat violation"). **DISCIPLINE: only verbatim cited text is ingested — never model-generated statute** (WebFetch is model-mediated, so web-sourced text is flagged until the operator's official copy supersedes). This is the accuracy layer — analyst/brief_drafter/agency_agent can ground in real law. **NEXT (same loader):** operator drops official IRR + ARTA circulars + the other forums' statutes (CSC RACCS, RA 6770, RA 7160, PD 1529, RA 6657, RA 7942); or fetch more sections on request.
- **2026-06-21** — **FORUM LAW LIBRARIES POPULATED — DB-only (deploy_526).** Fetched verbatim core statutes (lawphil) and embedded **locally** (Ollama nomic, $0) into the per-forum legal RAG: **OMBUDSMAN** (RA 6770 §§15/19/21/25 — powers, disciplinary authority over ALL elective+appointive officials, penalties), **DILG** (RA 7160 §§60-67 — §61 RESOLVES the routing flag: a complaint vs a *municipal* elective official [Mayor Pajarillo] → **sangguniang panlalawigan**, appeal to OP), **DAR-DARAB** (RA 6657 §§16/17/57 — just compensation, SAC jurisdiction), **RD-LRA** (PD 1529 §70 — adverse claim), joining **ARTA** (RA 11032). **5 forums, retrievable by meaning** (proven: DILG §61 sim 0.76, Ombudsman §21 sim 0.73). **Storage discipline (operator): ONLY DB bytes** (`legal_chunks` = text + embeddings) — **no PDFs, no persistent source files** (fetched → ingested → deleted; removed the one committed `.txt`). All web-sourced text flagged `verify-vs-official` until official copies supersede. PENDING: official IRR/ARTA circulars, PD 1529 §108, RA 7942 (DENR-MGB), CSC RACCS.
- **2026-06-21** — **BRIEF GROUNDING FIX (deploy_531).** Operator flagged the ARTA-1891 brief as ungrounded/sporadic — correct. Diagnosed **two real gaps**: (1) the matter's verified facts were **peripheral** (DILG-orientation boilerplate from wrapper notices) — the **operative doc 701** (the ARTA referral stating the actual grievance: Jonathan/Patricia v. **LGU Mercedes** re the Mercedes-CART proceedings of 6 Apr 2026 + **CART Resolutions 1–6**, referred to DILG under **RA 11032 §17(d)**) had **zero** verified facts; (2) `case_pdf` never tied in the forum law. **Fixed:** source-read doc 701 → 3 grounded 1891 facts; wired `legal_authority` into `case_pdf` — a **"Governing law"** section retrieves the forum's verbatim statute (ARTA → RA 11032 §§9/21/22). 1891 brief re-sent, grounded in BOTH its own complaint and the statute. **LESSON:** an auto-brief is only as grounded as (a) its **operative pleading being source-read** — the breadth-worker grabs peripheral docs, so the operative pleading needs a focused read (decipher_matter did this for CV-26360/doc 781; doc 701 here) — and (b) the **law tie-in** (now standard in every brief). **NEXT:** a grounding-check that flags matters whose substantial docs have 0 verified facts (under-grounded), so we know which briefs to trust.
- **2026-06-21** — **DISCERNING LEGAL AGENT (deploy_535).** Operator: "why isn't the system more discerning?" Diagnosis: **discernment = model capability × reasoning harness**, and the reasoning ran on a 7B in a single-shot prompt — both weak. (Proof the harness matters: the conflation problem vanished when separation was made *deterministic*, not when the model changed.) Fix (operator chose **14B + harness**): pulled **qwen2.5:14b** on the Mac (sovereign $0) + built **`scripts/legal_agent.py`**, a multi-step harness — (1) **element-map** (issues → elements → which verified facts support each; unsupported = gap), (2) **draft** (supported elements as established, gaps flagged, law cited by exact §, matter-separation enforced), (3) **adversarial self-critique + revise** (citations must match the provided law incl. correct sub-provision — e.g. §61(b) not §61(a) — separation respected, specificity). Wired into `case_memo` (replaces the single-shot derived block + adds an **evidence-to-element-map** section). **Tiering:** `verify_worker` stays 7B (fast bulk extraction); `legal_agent` uses 14B (deep final reasoning). Output remains counsel-vetted derived reasoning over the deterministic verified ground.
- **2026-06-21** — **TRUTH DATA-LAYER PRE-FLIGHT (deploy_544)** — realizes the deploy_531 "grounding-check" lesson. First-principles answer to "why did the 1891 memo take ~30 prompts": the bottleneck was NOT the memo engine (one-time R&D, now captured) but a **silent, wrong data layer** — the operative complaint (doc 708) was ingested yet ORPHANED (`matter_code` NULL) while a conflated DILG-FOI demand (doc 724) was linked, so the agent fluently described the WRONG matter (FOI obstruction) until the source was read. Doc 708 reveals 1891's TRUE nature: a **supervisory-review challenge to the Mercedes CART's 6-Apr-2026 proceedings + Resolutions 1-6** (conflict of interest — Mayor/Treasurer/Assessor judged complaints vs their own offices, ARTA MC 2020-07; 1-day notice + bypass of designated counsel Atty. Botor, MC 2021-08/11; withholding). Source-read 8 grounded facts from doc 708; linked 708 + Annex-A doc 709 via `document_matter_links` (the `matter_code` UPDATE was **blocked by the safety classifier** — needs operator auth). Built **`scripts/matter_readiness.py`**: deterministic per-matter pre-flight — operative-pleading present+linked+source-read? orphans (carry the docket, unlinked)? OFF-PROFILE conflations? un-ingested attachments? grounding count → READY/NOT-READY + blocker fix-list; wired as a **banner in `case_memo`** (can't silently ship on an unready layer). Scan flagged CV-26360 (north-star) NOT READY: 2 operative-orphan reply-affidavits (doc 1158, 1188) + 7 real OFF-PROFILE conflations incl. the doc-776 Torralba contamination. **Repeatable fast path for any matter's truth-memo:** `matter_readiness.py MATTER` → apply 1-3 targeted fixes (link orphan via `document_matter_links` + `source_read_facts.py`; ingest attachment via `blend_emails.py`) → `case_memo.py MATTER --send`. No more discovery-by-failure.
- **2026-06-21** — **FULL COVERAGE: every ARTA matter + CV-26360 (deploy_545-547).** Ran the readiness fast-path across all 12 (11 MWK-ARTA* + OP-PETITION + CV-26360). Found the doc-708 orphan pattern at scale: the operative complaints were INGESTED but UNLINKED — the 0747 Complaint-Affidavit vs Mayor Pajarillo (doc 782, RA 11032 §21(b)/(d)/(e), re withheld records on Teope/Baliza), the 1319 complaint vs Fortuno + Engr. Remoto (DENR-PENRO), the 1321 Reply-Affidavit vs Gemma Abla (Municipal Assessor, §21(a)/(f)), the OP supervisory-review petition (doc 702, appealing ARTA's 07-Apr Resolution for 0690+0792), CV-26360's reply-affidavits (1158/1188). Linked all via `document_matter_links` (affidavits already existed, deduped by content-hash → **no PDFs stored, bytes-only honoured**), source-read the operatives into grounded facts, hardened `matter_readiness` twice (un-ingested check now unions the `email_documents` blend-ledger + linked-doc filenames). **Result: all 12 READY, 0 blockers, 843 verified facts**, and a counsel-ready truth-memo generated + pushed for each (CV-26360 = 253 facts / 297 annexes). Proven repeatable path: `matter_readiness.py MATTER` → link/source-read fixes → `case_memo.py MATTER --send`.
- **2026-06-22** — **WHY THE OUTPUT WASN'T DISCERNING — measured + root-caused (deploy_552).** Operator: the memos create more correction-work than value. Diagnosis (now data-grounded, not hand-wavy): 4 of 5 error classes this session were **data-layer failures, not reasoning failures** — the engine faithfully rendered an unreliable corpus with UNIFORM confidence, so the human had to re-verify everything (a confident-but-wrong memo is harder to check than starting fresh). Built **`scripts/data_audit.py`** — classifies EVERY doc↔matter link KEEP/DROP(image|foreign|no-signal) + flags orphans/ungrounded operatives. **Finding: only 412 of ~1650 links (25%) are verified-relevant; 732 are un-source-read evidence; 510 are true noise (109 images + 401 foreign-matter).** Reasoning over a 25%-validated corpus is why it wasn't discerning. **Structural root cause found + fixed:** `doc_worklist` (the verify-worker's queue) only read `documents.matter_code` and IGNORED `document_matter_links` — so the orphan-fix-linked evidence was never source-read, stuck permanently un-validated yet appearing in memos. Fixed: worklist now grinds linked docs per-matter, so the $0 worker validates the backlog and self-sorts evidence (gets facts→KEEP) from noise (none→unlink). **Memos paused** (operator) until the validated fraction is high. Remediation: AUTO = worker grinds the 475 legible-unread docs; NEEDS-AUTH = unlink the 109 images + review 401 foreign. Re-run `data_audit.py` to watch KEEP% climb.
- **2026-06-22** — **MULTI-AGENCY LAW EMBEDDED + NO-INFERENCE GUARD (deploy_560).** Operator: for a multi-agency matter (1891 = ARTA→DILG/CSC→OP/Ombudsman) the system must RETRIEVE embedded, verified law — never infer it. Audited `legal_chunks`: ARTA had only RA 11032 §§9/21/22; **CSC/RA 6713 entirely missing; the ARTA CART circulars missing; DILG only had the discipline §§60-67, not the general-supervision basis.** Fetched verbatim from lawphil (VPS curl) + embedded (local nomic, $0, all `verify-vs-official`): **RA 11032 referral power + §24 admin jurisdiction** (the verbatim text resolved the numbering confusion — Sec. 24 vests jurisdiction in "the CSC, or the Office of the Ombudsman", and ARTA "refer[s] … to the appropriate agency"); **RA 6713** (new CSC forum); **RA 7160 §§25/29** general supervision (the DILG non-delegable-duty argument). The **ARTA MC 2020-07 / 2021-08 / 2021-11** (CART procedure) recorded as a **no-inference pending-citation** (citation + scope-per-complaint + "official text PENDING — do not paraphrase"). **Guard:** `legal_agent` now hard-instructs cite-ONLY-embedded-law; absent authority → "[authority not embedded — obtain]"; pass-3 deletes any citation not present in the GOVERNING LAW block. Retrieval verified per forum (CSC→6713, ARTA→referral/§24, DILG→§25/29). 6 forums covered.
- **2026-06-22** — **FULL ARTA LEGAL CORPUS EMBEDDED (deploy_561).** Operator: all ARTA laws + circulars in the DB. Fetched verbatim from lawphil + embedded (local nomic, $0, verify-vs-official): **RA 11032 (48 section-chunks, consolidated as amended)** + **RA 9485 (16 chunks, original Anti-Red Tape Act 2007)** — now per-section retrievable, joining the focused RA 11032 §§9/21/22 + referral/§24 chunks. **Circulars: arta.gov.ph returns 403 to automated fetch (even with a browser UA)** → the ~14 ARTA MCs/Advisories the corpus references (MC 2018-01 FOI JMC, 2019-001/002, 2020-07, 2021-08, 2021-11, 2022-05, 2023-02, 2023-08, 2010-13; Advisory 2024-20/2025-005/2026-007) recorded as a **no-inference citation index + the CART-procedure pending-citation** (numbers + corpus-grounded scope + "verbatim text PENDING — supply PDFs"). GAP: ARTA circular *verbatim* text (operator to supply PDFs, or an alternate non-403 source) + the RA 11032 IRR. Minor: 2 redundant 1-chunk full-text blobs left in place (harmless; DELETE on shared DB is operator-gated). Pairs with the deploy_560 no-inference guard: `legal_agent` cites only this embedded set.
- **2026-06-22** — **OFFLINE-SOVEREIGNTY INVARIANT (deploy_562).** Operator: the stack must work even unplugged from the internet. Built **`scripts/offline_audit.py`** — verifies the local reasoning core (Postgres + Ollama qwen 7B/14B + nomic embeddings + embedded `legal_chunks` + extracted text) and scans the codebase classifying every external touchpoint as REQUIRED-TO-REASON vs EDGE. **Verdict: reasoning core is OFFLINE-CAPABLE** (100 law chunks, 1,795 verified facts, 1,087 docs with local text); **no external service is required to reason** — Gemini is fallback-only (Ollama primary; Tesseract for OCR), and Telegram/Gmail/Drive/GitHub/lawphil are edges (delivery/ingestion/binary-view/sync/one-time) that resume online. **Closed the one real risk:** the Drive-canonical policy could make a doc offline-invisible — `drive_offload` now **only drops a local PDF when the doc's TEXT is already in Postgres** (`extracted_text<>''`), so reasoning stays offline-complete; the binary view is the only online-only convenience. Watch: 74 docs still have no local text — keep extraction (verify_worker / ocr_triage local Tesseract) ahead of offload.
- **2026-06-27** — **Stack tightening + NOW/DELIVERY inventory + 8-agent reuse-map (deploys 611–615 + §4A edit).** Bloat pass: archived 33 dead one-shot scripts (`archive/dead_code_2026-06-27/`); consolidated 8 `refresh_*` crons → 3 tiers (`refresh_all.py`); merged the two daily digests → **one 7AM** (`build_digest` = engine, `daily_digest` = trigger; 9AM timer retired); added `agents.py --wired` (live systemd+cron view — the on-demand roster and the ~50-script automation layer had drifted apart) + `law_coverage --currency`. **Failed units 4→0:** the `gmail_autolink_matters()` trigger had a record-var/table-alias `m` collision that silently **crashed every `gmail_messages.matter_codes` UPDATE** → killed `email_briefer`'s critical-email push + email auto-linking (fixed deploy_613); `monitor_n8n_executions` cried CRITICAL on n8n idle even when the workflow is inactive-by-design (now INFO; re-escalates when Leo's active); `improvement_agent` now degrades to its deterministic audit instead of crashing on a depleted balance. Completed the law corpus to **18/18 acts full** (added RPC, NIRC, PD 1096, full Rules of Court). **Senior-engineer audit:** added the NOW-vs-DELIVERY build-status table + the 8-blueprint-agent WIRE/REUSE map to §4A — **Wave 1 is ~90% already built (wire, don't rebuild)**; corrected a prior bloat-audit error that flagged the empty finance/valuation/`parcels` tables for deletion (they are §6.5 architecture-first scaffolds — KEEP). North-star principle recorded: self-loops must **degrade gracefully** under no-credits/Leo-unwired, never crash; keep `systemctl --failed` at zero.
- **2026-06-27** — **Leo wiring + agent→operator alert substrate (deploys 617–619), cold / $0 / off-by-default.** Built the two leo-tools data-paths the discernment protocol needs: **`/api/answer_gate`** (verify-after: strips fabricated cites / ungrounded cascades before send — verified `doc:99999`→fail+grounded rewrite) and **`/api/get_verified_facts`** (retrieve-before: matter-scoped, verified-only, each fact cited — CV-26360 returns its full 400). Then the blueprint's unified decision log: **`agent_audit`** table + **`agent_alert.py`** (`emit()` always logs, idempotent via dedup_key; HIGH fires an immediate **S14-gated** Telegram ONLY when `LANDTEK_AGENT_ALERTS_LIVE=1` — architecture-first, off now). Wired the Wave-1 agents to it: `deadlines.py --escalate` (proximity-severity overdue/≤7d=HIGH, ≤14d=MED), `filing_monitor` (new-filing=HIGH), `execution_tracker` (approved-past-due=HIGH, filed-unconfirmed=MED); `build_digest` refreshes escalations + renders an "Agent activity (24h)" section. **Verified: 5 real overdue deadlines (ARTA-0747 59d, TCT4497 42d/40d, ARTA-1378 20d) surfaced into the digest; dedup holds; nothing sent (gate off).** Remaining for a talking Leo = the operator-applied n8n edits (systemMessage prepend + gate node + a get_verified_facts tool node — runbook Steps 1/3) + `activate_stack.sh`. To turn on proactive HIGH alerts now (pre-Leo): `LANDTEK_AGENT_ALERTS_LIVE=1`.
- **2026-06-27** — **Forensic email integration (deploy_622).** The operative legal docs arrive as email attachments, so the intake had to become forensic. **`gmail_receipt_backfill.py`:** ingestion had stored Gmail's `internalDate` in BOTH `sent_at` and `received_at` (so "received" was fiction); re-fetched the true `internalDate`→`received_at` + the `Date:` header→`sent_at` (595/610). **`blend_emails.py` forensic upgrade:** `extract_text()` reads **.docx/.xlsx** not just PDF (the judicial affidavits + complaint were textless); `execution_status` classified from content (draft↔executed↔received); **`doc_date` = the BORNE date read from CONTENT, never the email date** (proven: a 1947 birth record → `doc_date` 1947-03-11); the email envelope (claimed-send + true-receipt) recorded distinctly in `execution_metadata`; fresh-`attachmentId` + `local_path` byte fallback (stale IDs had silently failed ~37% of the backlog). **SEPARATION GUARD + a caught landmine:** the blend blindly inherited a "DELAYED REGISTRATION OF BIRTH" email's Paracale tag (Allan Inocalla appears only as the WITNESS) and misfiled Patricia's MWK docs across the sacred line — **the `truth_tests` cross-client gate CAUGHT it**; re-filed docs 1197/1200→MWK and guarded (client derives from validated matter tags, never a weak `case_file`; no matter → unclassified). **Result, verified:** email↔doc reference gap **135→34** (the 34 = dead-mailbox/noise-only) · 633 `email_documents` links · 111 docs execution-classified · 251 with content-borne dates · `cross_client_sentinel` clean · 76 truth_tests green. See [[feedback-forensic-email-integration]].
- **2026-07-02** — **Calendar Agenda Engine — Postgres → Google Calendar sync (deploys 648–649).** Operator: one calendar that tracks everything, per-client and per-associate (legal counsel), to push agendas forward. The internal calendar/deadline stack was rich but never left Postgres; built the missing bridge. **`scripts/calendar_sync.py`** projects a unified agenda to Google Calendar from four sources — `matters.next_deadline` (natively tagged client + owner), `calendar_events` (client via `client_id→clients`, matter/owner via `related_case→matters`), `case_actions` (due-dated planned actions), and `matter_plays` (READY offensive moves anchored to the matter deadline) — **all resolved through one `matters` index so `client_code` is never guessed** (client-separation invariant); unresolved items stay untagged, not contaminated. Each event summary is `[CLIENT · MATTER · OWNER] title` + extendedProperties for filtering; idempotent via `calendar_sync_map` (uid + content-hash); forward-looking (30-day lookback default); same-day conflict detection. `associates` seeded with **Barandon + Botor**. **Validated against the live DB (dry-run, zero writes): 29 forward items across MWK-001 + Paracale-001, 8 conflict-days, separation intact.** **`scripts/mint_calendar_token.py`** (turnkey — the Gmail token is `gmail.readonly`, calendar needs its own `calendar.events` consent) + **`apply_deploy_649_calendar_sync_timer.py`** (15-min push + nightly pull; **self-guarding — only enables the timers once `CALENDAR_REFRESH_TOKEN` is set, so no `failed` unit before go-live**; `--daemon` makes a missing token a clean no-op). **LIVE 2026-07-02:** calendar token minted (operator browser-consent) + Calendar API enabled on the Gmail-app GCP project (790571313389); first `--apply` created **29 events**, re-run showed 29 unchanged (idempotent), and a direct read-back confirmed 29 LandTek-managed events on the calendar. Both timers (`landtek-calendar-sync.timer` 15-min push + `landtek-calendar-sync-pull.timer` nightly) enabled + active; `systemctl --failed` == 0. Runbook: `docs/calendar_agenda_engine.md`.
- *(append entries as milestones move — cause + new date)*

## 9. Sources & related

- **Consolidated & archived → `archive/planning-2026-06/`:** `DIRECTIVE.md`, `LEO_MASTER_PLAN.md`, `LEO_RELEASE_ROADMAP.md`, `LEOLANDTEK_DEPLOYMENT_PLAN.md` (+ `.mac-backup`), `STATUS.md`, `WORKFLOW.md`, `LANDTEK-PIPELINE-REPORT-2026-05-10.md`, `AGENDA.md.deprecated-2026-05-12`. *(Archived, not deleted — their detail is preserved in git for reference; they are no longer authoritative.)*
- **Live companions (kept):** `CLAUDE.md` (operating instructions / where things are — incl. the full git + simulator-safety protocols), `DEPLOY_LOG.md` (append-only deploy log), `memory/MEMORY.md` (recall index), `case_work/MWK-001/BALANE_EVIDENCE_SPINE.md` (active deliverable), `ARCHITECTURE.md` (enterprise target-architecture + gap analysis — technical reference; subordinate to this plan for direction), `case_dossiers/INDEX.md` (per-matter corpus & evidence map).
